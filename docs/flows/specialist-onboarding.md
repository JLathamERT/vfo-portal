# Specialist onboarding flow

A multi-stage workflow for vetting a new specialist (third-party expert). Five stages: 1 Preliminary Meeting, 2 Detail Meetings, 3 Due Diligence, 4 Contract & Details, 5 Going Live. Each stage has a task checklist, meeting log, and exec-vote tally on the onboarding row + child tables.

> **2026-06-02:** Stages **1–2 automated** (template emails, SIF form page, per-meeting rev-share proposal, notifications).
> **2026-06-03 (v374):** added **Stage 2 account-scoped exec voting** (2 rounds), **reviewer notes** (Tracy + Tim), **decline-on-deny**, the full **Stage 3 background-check payment chain** (Stripe ACH/Card $350/$950, confirmation + invoice/receipt PDFs, Drive archival, Further Questions flow, auto-advance to Stage 3), the **Specialist Automation Panel**, and a **reminder sweep** (`specialist-sweep-daily` 14:20 UTC Mon–Fri). Stages 4–5 remained a manual checklist. Specialist pipeline is in **sandbox** (`pipeline_sandbox_config` SPECIALIST_ONBOARDING `sandbox_mode=true`) — flip to live before real payments.
> **2026-06-04 (v394, branch `claude/epic-robinson-b8609d`, DEPLOYED + frontend live but NOT committed):** **DDC form rework** (sectioned, per-question cards, file/text/both inputs, tax-only Compliance section, nothing required), **3 Stage-3 completion emails** (`SPECIALIST_bg_passed` 89 / `SPECIALIST_ddc_approved` 87 [now written] / `SPECIALIST_revshare_complete` 90 via shared `utils/specialist-stage3-emails.ts`), the **all-three Stage-3→4 gate** (`maybeAdvanceStage3`), and **Stage 4 final executive approval matching Stage 2** (reviewer notes → 2-round voting → View details). **Frontend is now deployed** (whole Specialist UI live).
> **2026-06-05 (v404 / boldsign-webhook v39, branch `claude/keen-stonebraker-cacda8`, backend DEPLOYED, ⚠️ frontend NOT deployed, NOT committed):** **Stage-4 sign / pay / recurring-license flow built.** Both-exec-Approved auto-sends the agreement via BoldSign (`[PROPOSED_REVENUE_SHARE]` filled from `rev_share_final_text` || the Stage-2 `rev_share_prepared` notes) → CEO countersign → **$99/mo Stripe SUBSCRIPTION** (`/specialist-pay?kind=license`) → confirmation → invoice/receipt → **Stage 4→5**, recurring monthly thereafter. 8 new handlers, 20 new `specialist_onboarding` columns, `boldsign-webhook` specialist branch, 3 `webhooks.ts` subscription blocks, reminder sweep extended to 7 stalls, Stage-2 reviewer-notes reworked (rev-share→notes→vote, always-visible, Tracy "submit proposal" action). See the **Stage 4** section below.
> **2026-06-10 (branch `claude/confident-antonelli-048be5`, backend DEPLOYED v453, ⚠️ frontend NOT deployed, NOT committed):** Six onboarding tweaks. (1) **Email required** when creating an onboarding (`create.ts` validates format; the New Onboarding form disables Create until valid). (2) **Admins can add/remove** in a specialist's vault (new `specialist_vault_admin_upload_url` / `specialist_vault_admin_delete`; +2 actions → **291 total**). (3) **Tim's tax-risk notes are entered once** — required before the **Stage-2** vote only; fully removed from **Stage 4** (input, vote gate, AND the exec "View details" bundle). (4) **"Add to Showroom" auto-fills** `short_bio` (SIF strategy/expertise), `long_bio` (DD Professional Bio) and `D&B_revenue_share` (finalized proposal); the **"Specialist bios added" checkbox is removed** and completion is now gated on `headshot_added` alone. (5) The Stage-5 **"Open specialist →"** link works on every click (nonce + remount fix — gotcha #107). (6) The agreement WHEREAS line carries a new **`[STRATEGY_EXPERTISE]`** placeholder filled from the SIF (template id 12). Plus: the SIF **Strategy/Expertise field is a single line capped at 85 chars** (front- + back-end) so it never wraps in the contract, and the **duplicate license-confirmation-email** bug is fixed via an atomic claim (gotcha #108).
> **2026-06-10 (branch `claude/elegant-pare-015a4c`):** **Stages 4–5 go-live + the specialist portal built.** Stage 4: the `SPECIALIST_lic_invoicereceipt` body (id 96) was filled (was **"TBC"**); the first invoice raises two Tracy action-required notifications (Skool + Create-specialist). Stage 5 became automated: **Send invite to VFO Skool** (`automation_SPECIALIST_skoolinvite`, template `SPECIALIST_skool_invite` id 149) + **Create the VFO Specialist** (`automation_SPECIALIST_createspecialist` — builds the `experts` directory row FROM THE SIF, copies DD files to `specialist-documents/<expert_id>/`, chains the login email). New **specialist portal** = a 4th login type (role `specialist`): login-setup chain (`SPECIALIST_login_setup` id 150 / `SPECIALIST_skool_invite` id 149) writing to a new **`specialist_logins`** table, a single-tab Vault on a private **`specialist-documents`** bucket namespaced by `expert_id`, admin Vault view under Search Specialists. Completion (`status='completed'`) now gated on both stage-5 checkboxes `headshot_added` + `bios_added`. Migration `specialist_login_and_expert_link`. **Specialists are NOT members** — `experts` + `specialist_logins` only, no `members` row. See the **Stage 5 — Going Live** section below.

- **Pipeline label:** `SPECIALIST_ONBOARDING` (`email_templates.pipeline`, `agreement_templates.pipeline`, `notifications.pipeline`, `pipeline_sandbox_config.pipeline`).
- **NOT this flow: [License Fee Continuation](specialist-license-continuation.md)** (2026-08-24). An EXISTING, already-live specialist moved onto the portal's $99/mo ACH licence rides `specialist_onboarding` rows flagged `license_continuation=true` and REUSES the Stage-4 licence engine below, but it is a separate end-to-end flow with its own entry point (Accounting tab), its own pipeline label for templates (`SPECIALIST_LICENSE_CONTINUATION`), an admin-chosen charge day, an ACH-only checkout branch and **no stage progression at all**. Everything in this file about Stages 1-5 is onboarding-only.
- **State tables:** `specialist_onboarding` (main; `current_stage` 1–5, `status`, `background_check_type`, `sif_token`/`sif_data`/`sif_submitted_at`, the `bg_*` background-check payment cols, `further_questions_*`, and the reminder timer-guard cols — see [tables/specialists.md](../tables/specialists.md)), `specialist_onboarding_progress` (task checklist + `tracy_general_notes`/`tim_tax_risk_notes`/`rev_share_prepared` notes), `specialist_onboarding_meetings` (Stage 2 meetings + per-meeting rev-share proposal cols + reminder guards), `specialist_onboarding_votes` (now keyed `(onboarding_id, stage, voter_name, vote_round)`).
- **Exec voters / admin logins:** Anton Anderson (`aanderson@elitert.com`), Paul Latham (`platham@elitert.com`); **both** reviewer-note prompts (general AND tax-risk) go to Tracy Miller (`tnmiller@elitert.com`) as of 2026-07-27 — Tim Gacsy (`tgacsy@elitert.com`) left the company and his `allowed_admins` row is gone (gotcha #291). The remaining three are `allowed_admins` rows (gotchas #60, #67).
- **Trigger:** AdminPortal → Specialists dropdown → Onboarding → `components/admin/SpecialistOnboarding.jsx`.
- **All automated emails are Gmail DRAFTS** (match advisor/tax). Storage: public bucket `specialist-onboarding-assets`.

## Manual tracker handlers (`actions/onboarding/`)

| Action | File | Notes |
|---|---|---|
| `load_onboardings` | `load-list.ts` | All rows, newest first |
| `create_onboarding` | `create.ts` | New row, `current_stage=1`, `status='active'`. **Email required + format-validated (2026-06-10).** |
| `load_onboarding` | `load.ts` | Row + progress + meetings + votes (`select('*')` → includes new sif/rev cols) |
| `save_onboarding_progress` | `save-progress.ts` | Upsert `(onboarding_id, stage, task_key)`; **empty status defaults to `'completed'`** (see gotcha #62) |
| `save_onboarding_meeting` | `save-meeting.ts` | Append-only meeting insert; now also accepts `rev_proposal_text` → mints `rev_proposal_token` |
| `save_onboarding_vote` | `save-vote.ts` | Upsert `(onboarding_id, stage, voter_name, vote_round)`. **No longer used (2026-06-04)** — Stage-2 AND Stage-4 exec votes both go through `automation_SPECIALIST_execvote` now. |
| `update_onboarding` | `update.ts` | Advances `current_stage` / sets `status` / `background_check_type`. No auto-advance. **2026-08-26: `status` now has a positive allowlist — `'active'` \| `'stopped'` \| `'completed'`, anything else 400s** (hardening; the column is bare `text` with no CHECK, #431). It is also the backing action for the new **Live/Stopped header toggle** in `SpecialistOnboarding.jsx`'s detail view, which sits deliberately OUTSIDE every `isStopped`-disabled region — it is the only control that can bring a stopped onboarding back — and is hidden once `status === 'completed'`. |

All of the above are `ADMIN_ONLY_ACTIONS`.

## Automation handlers (`actions/onboarding/`)

| Action | Auth | Trigger | Does |
|---|---|---|---|
| `automation_SPECIALIST_prelimemail` | AUTH | Stage 1 decision buttons | `continue`/`continue_no_date` → drafts `SPECIALIST_yes` (process image + SIF button + 2 static PDF attachments + closing); mints `sif_token`; stamps `sif_email_sent_at`. `stop` → `SPECIALIST_no`. **Accepts `reschedule` (2026-08-16, #404): when true it re-sends the same email with a new slot but does NOT re-stamp `sif_email_sent_at`**, because that column is the SIF reminder + Tracy-bell ladder's clock in `actions/onboarding/sweep.ts`. The FE also does **not** `advanceStage` on a reschedule. |
| `automation_SPECIALIST_stage2email` | AUTH | Stage 2 "Still interested — Send email" | Drafts `SPECIALIST_step2_progress` (Completed/Remaining lists + process image + date closing); "all areas complete" closing when nothing remains. (Per-meeting rev-share proposal removed 2026-06-04.) **Accepts `reschedule` (2026-08-16): when true it skips Tracy's `allDone` revenue-share-proposal bell** — the original send already raised (or already resolved) that ask. |
| `automation_SPECIALIST_loadsif` | PUBLIC | `/specialist-sif` load | By `sif_token` → prefill + existing `sif_data` |
| `automation_SPECIALIST_submitsif` | PUBLIC | `/specialist-sif` submit | Saves `sif_data` (incl. the tax-specialist Q + 4 tax-risk answers) + `sif_submitted_at`; notifies Tracy (FYI) |
| `automation_SPECIALIST_revsharefinal` | PUBLIC (token) | `/specialist-revshare-final?token=&decision=Approved\|Questions` | Specialist's response to the Exhibit A proposal in the Step 3 email → `rev_share_final_*`; Approved → Tracy FYI, Further Questions → non-dismissible Tracy action notif. **The page shows a confirmation card and records nothing until the specialist clicks it** (2026-07-27, gotcha #290); it keeps its own non-`TokenShell` layout. |
| `automation_SPECIALIST_revsharefinalize` | AUTH (admin) | Stage 3 Confirm/Edit rev-share buttons | Confirm → `rev_share_finalized` notes `'kept'`; Edit → edited text to `rev_share_final_text` (preserves original `rev_share_prepared`, gotcha #70) + `'edited'`; clears Tracy's action notif |
| `automation_SPECIALIST_loadddc` | PUBLIC (token) | `/specialist-ddc` load | By `ddc_token` → prefill + `ddc_data` + `ddc_review_status`/`ddc_edits_reason` + is-tax flag |
| `automation_SPECIALIST_saveddc` | PUBLIC (token) | `/specialist-ddc` autosave | Saves whole `ddc_data` (text + file refs); no submit stamp |
| `automation_SPECIALIST_ddcuploadurl` | PUBLIC (token) | `/specialist-ddc` file pick | Mints a signed UPLOAD url into the private bucket `specialist-dd-materials` (gotcha #69) |
| `automation_SPECIALIST_submitddc` | PUBLIC (token) | `/specialist-ddc` "Submit for review" | `ddc_submitted_at` + `ddc_review_status='pending_review'`; appends `ddc_review_log` `{submitted}`; Tracy FYI |
| `automation_SPECIALIST_ddchelp` | PUBLIC (token) | `/specialist-ddc-help` | `ddc_help_requested_at`; non-dismissible Tracy action notif (clears on "Help received" → `save-progress` `ddc_help_resolved`) |
| `automation_SPECIALIST_ddcapprove` | AUTH (admin) | Stage 3 "DD Checklist Approved" | `ddc_review_status='approved'`; appends `ddc_review_log` `{approved}`; drafts `SPECIALIST_ddc_approved` (now written — reports the other Stage-3 items via the completion-email helper); calls `maybeAdvanceStage3` |
| `automation_SPECIALIST_ddcedits` | AUTH (admin) | Stage 3 "Denied" (+ reason) | Drafts `SPECIALIST_ddc_edits` (reason + resubmit link); `ddc_review_status='edits_requested'`, clears `ddc_submitted_at`; appends `ddc_review_log` `{denied,reason}` |
| `automation_SPECIALIST_ddcdownload` | AUTH (admin) | Stage 3 "View progress" file link | Mints a 300s signed DOWNLOAD url for one DD file (path prefix-checked, gotcha #69) |
| `automation_SPECIALIST_execvote` | AUTH (admin) | Stage 2 **and Stage 4** exec approval buttons | **Stage-aware (`body.stage`).** Caster derived from session; round 1 Approved/Further-Questions, round 2 Approved/Denied. Stage 2 both-Approved → chain `step3email` + advance Stage 3; **Stage 4 both-Approved → auto-chain `automation_SPECIALIST_sendagreement`** (backgrounded via `EdgeRuntime.waitUntil`); both-Denied → `deniedemail` + stop; split → reset + re-vote notif; fires "vote completed" FYI. (Reveal for stages 2 & 4 in `load.ts`, gotcha #64.) |
| `automation_SPECIALIST_deniedemail` | PUBLIC (svc-role/admin) | Chained on both-Denied | Drafts `SPECIALIST_denied` (idempotent on `denied_email_sent`) |
| `automation_SPECIALIST_step3email` | PUBLIC (svc-role/admin) | Chained on both-Approved | Creates Stripe customer + `bg_checkout_token` + `further_questions_token`; drafts `SPECIALIST_step3` (Core/Max/Questions buttons); advances `current_stage` to 3 |
| `automation_SPECIALIST_bgloadpayment` | PUBLIC (token) | `/specialist-pay` load | Returns name + Core/Max type + amount ($350/$950) |
| `automation_SPECIALIST_bgcheckout` | PUBLIC (token) | `/specialist-pay` ACH/Card pick | Stripe Checkout one-time, metadata `pipeline=SPECIALIST_ONBOARDING`/`payment_kind=background_check`/`bg_type` |
| `automation_SPECIALIST_bgconfirmation` | PUBLIC (svc-role) | Webhook at payment time — **ACH ONLY** (`!isCardS` gate at the call site) | Drafts `SPECIALIST_bg_confirmation\|ach` (MAP1 wording). A **card** background-check payment gets the receipt only, since it clears instantly and `bgreceipt` fires in the same moment — the `\|card` template is no longer sent automatically |
| `automation_SPECIALIST_bgreceipt` | PUBLIC (svc-role) | Webhook on clearance (card now / ACH on settle) | Invoice+receipt PDFs → Drive `VFO Specialist Onboarding/<Name> - Specialist`; **mints `ddc_token` + `rev_share_final_token` + stamps `ddc_email_sent_at`**; drafts the full `SPECIALIST_bg_receipt` (DD Checklist + Request-Help buttons, process image, Exhibit A proposal + Happy/Further-Questions buttons) w/ both PDFs attached; marks paid; Tracy FYI; sets Checkr/Scherzer label |
| `automation_SPECIALIST_questionsrequest` | PUBLIC (token) | `/specialist-questions` | Records request + non-dismissible Tracy notif |
| `automation_SPECIALIST_questionsresolve` | AUTH (admin) | Tracy's Proceed/Stop buttons | Proceed → `SPECIALIST_step3_proceed`; Stop → `SPECIALIST_no` + stop; clears Tracy's notif |
| `automation_load_specialist_pipelines` | AUTH (admin) | Automation tab loader | All rows + votes + meetings + final-proposal + sandbox config |
| `automation_SPECIALIST_sweep` | PUBLIC (svc-role) | cron `specialist-sweep-daily` 14:20 UTC Mon–Fri | 2-business-day reminder / 4-business-day Tracy FYI for **7 stalls** (gotcha #66) |
| `automation_SPECIALIST_sendagreement` | PUBLIC (svc-role/admin gate) | Auto-chained from `exec-vote` on Stage-4 both-approved | Renders agreement HTML→PDF (`[PROPOSED_REVENUE_SHARE]` + `[STRATEGY_EXPERTISE]` ← `sif_data.strategy_expertise` filled), coordinate-based BoldSign send (signer 1 specialist, 2 Anton), drafts `SPECIALIST_agreement_sent`; idempotent on `agreement_sent_at` |
| `automation_SPECIALIST_ceocountersign` | PUBLIC (svc-role/admin) | BoldSign webhook (specialist signed) | Fetches Anton's embedded countersign link, drafts `SPECIALIST_ceo_countersign` |
| `automation_SPECIALIST_licstripecustomer` | PUBLIC (svc-role) | BoldSign webhook (Completed) | Reuses `bg_stripe_customer_id`, mints `lic_checkout_token`, chains `licpaymentemail` |
| `automation_SPECIALIST_licpaymentemail` | PUBLIC (svc-role) | Chained from `licstripecustomer` | Drafts `SPECIALIST_lic_payment` w/ `/specialist-pay?kind=license`; marks `payment_link_sent` |
| `automation_SPECIALIST_licloadpayment` | PUBLIC (token) | `/specialist-pay?kind=license` load | Returns name + $99/mo |
| `automation_SPECIALIST_liccheckout` | PUBLIC (token) | `/specialist-pay?kind=license` ACH/Card pick | Stripe Checkout **`mode=subscription`** $99/mo (card grossed-up / ACH flat), metadata `payment_kind=license` |
| `automation_SPECIALIST_licconfirmation` | PUBLIC (svc-role) | Webhook at license payment time — chained on both paths, but **sends for ACH ONLY** | Drafts `SPECIALIST_lic_confirmation\|ach`. **The card gate lives INSIDE the handler**, not at the call sites, because TWO racing webhook paths chain it (`checkout.session.completed` + the invoice-paid processor) and both must obey the same rule; on card it returns `{ok:true, skipped:"card - receipt only"}` **without** stamping `lic_confirmation_email_sent_at`, so a manual resend stays possible. A card licence gets the monthly invoice/receipt the moment the charge clears. **Idempotent via an ATOMIC claim** on `lic_confirmation_email_sent_at` (conditional update where-null) — the subscription fires `checkout.session.completed` + `invoice.paid` together and both chain this, so a read-then-write guard raced into 2 emails (gotcha #108, 2026-06-10) |
| `automation_SPECIALIST_licinvoicereceipt` | PUBLIC (svc-role) | Webhook on each `invoice.paid` | `INV-SPECLIC`/`REC-SPECLIC` PDFs → Drive (license reuses the **bg** invoice/receipt HTML generators); drafts `SPECIALIST_lic_invoicereceipt` (id 96, **now written** — `license-invoice-receipt.ts` substitutes `[FIRST_NAME]`/`[AMOUNT]`/`[MONTH_LABEL]`) w/ both attached. On the **FIRST** invoice (subscription create) also raises **two** Tracy action-required notifs (Skool + Create-specialist; see Tracy ladder) + advances Stage 4→5. |
| `automation_SPECIALIST_skoolinvite` | AUTH (admin) | Stage 5 "Send invite to VFO Skool" | Admin pastes the Skool invite link → drafts `SPECIALIST_skool_invite` (id 149; inline `onboarding-process.png` + `[SKOOL_LINK]` button), the Step-4-completed/Go-Live email; marks `skool_invite` progress; clears the "Send VFO Skool invite" Tracy notif |
| `automation_SPECIALIST_createspecialist` | AUTH (admin) | Stage 5 "Add to Showroom & Send Specialist Login" | Builds an `experts` directory row FROM THE SIF (see mapping below — incl. **`short_bio`←`sif_data.strategy_expertise`, `long_bio`←DD `ddc_data.bio`, `D&B_revenue_share`←finalized rev-share**, 2026-06-10); copies DD-checklist files → `specialist-documents/<expert_id>/`; writes back `expert_id`/`expert_created_at`; chains `loginsetupemail`. Idempotent on `expert_id`. Clears the "Create the VFO Specialist" Tracy notif + raises **the headshot action** (bios auto-filled — no bios notif) |
| `automation_SPECIALIST_loginsetupemail` | PUBLIC (svc-role/admin) | Chained from `createspecialist` | Drafts `SPECIALIST_login_setup` (id 150, link `/member-setup?token=`); mints `login_setup_token` + stamps `login_setup_email_sent_at` |
| `automation_SPECIALIST_loadloginsetup` | PUBLIC (token) | `/member-setup?token=` load (specialist fall-through) | By `login_setup_token` → name/email prefill |
| `automation_SPECIALIST_submitloginsetup` | PUBLIC (token) | `/member-setup` submit | Writes the passcode to **`specialist_logins`** (NOT `member_logins`); stamps `login_setup_completed_at`; redirects to `/specialist/login` |
| `specialist_vault_list` / `_upload_url` / `_download` / `_delete` | AUTH (specialist) | Specialist portal Vault tab | Scoped to `auth.callerSpecialistId`; private bucket `specialist-documents/<expert_id>/` |
| `specialist_vault_admin_list` / `_admin_download` / `_admin_upload_url` / `_admin_delete` | AUTH (admin) | Admin Search Specialists → Vault tab | Admin-only, by `expert_id` (path prefix-checked). Admins can **view, add, and remove** (upload/delete added 2026-06-10) |

## Stage 1 — Preliminary Meeting

Admin picks (MAP1-style row, "Sending…" lock): **Continue — Send email (with date)** (date + time + US-timezone picker), **Continue — date not yet arranged**, or **Stop — Send email**.
- Continue → `SPECIALIST_yes` draft + `sif_token`. The **AI PC Admin** sub-section shows *Email sent… (SIF form, rev share examples, template agreement attached)* + *SIF form completed by potential specialist* (green + expandable to all SIF answers once `sif_submitted_at` set).
- Specialist clicks **Complete the SIF Form** → `/specialist-sif?token=<sif_token>` (all fields required except Company Name, Website URL, and the two "if applicable" fields) → submit → `sif_data` + Tracy notified.
- Stop → `SPECIALIST_no` decline draft, no AI PC Admin block.

## Stage 2 — Detail Meetings

- 9-item checklist; ticking is **toggleable** until the meeting is logged (`'unchecked'` sentinel, gotcha #62).
- **Revenue Share Proposal** sits in Stage 2, before "Initial executive approval." The proposal box (manual entry, "Submit revenue share proposal") is what Anton & Paul vote on. **As of 2026-06-12 it behaves like the reviewer-notes box** (reuses `ReviewerNoteInput`): after submit the textarea stays on screen (badge "Awaiting proposal" → "Submitted") and is editable via "Update revenue share proposal" — the old submit-then-collapse `RevShareDisplay` (▶ View proposal toggle) was removed. The Stage-3 "Final revenue share proposal" Confirm/Edit flow is separate and unchanged.
- **"Discuss revenue share (detail)"** is now just a **plain checklist item** (the per-meeting proposal textarea + email Approve/Propose-edit buttons + per-meeting "AI PC Admin" block were **removed 2026-06-04** — the specialist's rev-share sign-off moved to the Step 3 receipt email).
- **"Still interested — Send email"** logs the meeting then drafts the Stage 2 email (save-meeting first, then stage2email with `meeting_id` — gotcha #63). Buttons collapse to a single "Still interested" (no date/no-date) when the current checks complete ALL items.
- **"Resend with new date" (2026-08-16, v747)** sits on the **newest logged meeting only** — earlier rows are settled history whose "next meeting" already happened. It re-sends the same email with `reschedule:true` (so Tracy's bell is not re-rung) and **edits the existing meeting row in place rather than logging a second one**: `save_onboarding_meeting` gained an optional **`meeting_id`** that switches it from INSERT to a scoped UPDATE (the row must belong to the named `onboarding_id`, else 404; only supplied keys are written, so a partial edit cannot blank the rest). **No new action was added** — the update mode lives inside the existing one. The Completed / Remaining lists are rebuilt from the items that meeting recorded, so the re-sent email matches the original.
- **Stop — Send email** logs a stopped meeting + fires `SPECIALIST_no`. Once stopped the live remaining-items checklist is read-only.
- **SIF tax branch:** the SIF form's last question is "Are you a Tax Specialist?" Yes/No. Yes reveals 4 required tax-risk questions (general risks / history / worst-case / precautions), all stored in `sif_data`. Tax specialists additionally require the **tax-risk notes** below (Tracy's, since 2026-07-27; the stored progress `task_key` is still the historical `tim_tax_risk_notes` and must NOT be renamed).

## Stage 2 — Reviewer notes → Executive approval (2-round voting)

1. **Final revenue share proposal** submitted (the box, → `rev_share_prepared`). On submit, fires non-dismissible action notifications: **Tracy** (general notes, always) + **Tracy again** (tax-risk notes, only if tax specialist — was Tim until 2026-07-27). They clear when the matching notes save — and because both prompts now share one recipient, the clear matches on the exact TITLE, not recipient+link (gotcha #291).
2. **Initial executive approval opens** once final proposal + the general notes + (the tax-risk notes, if tax) are all done → non-dismissible "vote needed" to Anton + Paul, and `vote_r1_opened_at` is stamped. **Order-independent as of 2026-07-08** (`maybeOpenExecVoting()` in `save-progress.ts` runs on BOTH the proposal save and the notes saves — before that, completing the notes before the proposal never opened voting; gotcha #195. Stranded rows on a pre-fix deploy: re-save the notes task).
3. **Round 1:** each exec clicks **Approved** or **Further Questions**. A vote's value is **private until both vote** (server-side redaction in `load.ts`, gotcha #64) — others see "Voted — awaiting". A **View details ▼** dropdown bundles SIF answers + final proposal + the general and tax-risk notes.
4. **Outcomes:** both **Approved** → auto-draft `SPECIALIST_step3` + advance to Stage 3. Any **Further Questions** → **round 2** opens (`vote_r2_opened_at`) with **Approved / Denied** + "second decision needed" notif. Round 2 both **Approved** → Stage 3; both **Denied** → `SPECIALIST_denied` decline + `status='stopped'`; **split** → round-2 votes reset + "re-vote" notif. On either round completing, an "Executive voting completed" FYI goes to Anton + Paul + Tracy.

## Stage 3 — Due Diligence: payment → DD checklist review → final rev-share

The Stage 3 admin tracker (`SpecialistOnboarding.jsx`) has a payment lead-in, then **Background Check**, **Due Diligence Checklist**, and **Final Revenue Share Proposal** sections, plus a **"Stage 3 progress"** rollup (green/grey/red bullets). **Gate (2026-06-04): Stage 3 → Stage 4 requires ALL THREE — background-check Passed AND DD checklist Approved AND rev-share finalized** (`maybeAdvanceStage3`; the backend owns the advance and is called from every completion handler). Each of the three completions also drafts a **completion email** to the specialist (see "Stage 3 — completion emails" below). The old "Step 3 complete, moving to Step 4" auto-step was removed.

**Payment lead-in:**
- **`SPECIALIST_step3` email** (auto-sent on exec approval) has **Core $350** / **Max $950** / **I Have Further Questions** buttons.
- **Core/Max** → `/specialist-pay?token=<bg_checkout_token>&type=core|max` → ACH-or-Card picker → Stripe one-time. **Card:** `bgreceipt` fires immediately and **no confirmation email is sent** — the card has cleared and the receipt says everything the confirmation would. **ACH:** confirmation at authorization, **receipt on clearance** (2–4 days, via `payment_intent.succeeded`).
- **I Have Further Questions** → `/specialist-questions` → non-dismissible Tracy notif → admin **"Answer the specialist's background check questions"** bullet (stays as a paper trail) with **Questions answered** (re-send `SPECIALIST_step3_proceed`) / **Stop** (`SPECIALIST_no` + stop).
- Payment failure → `bg_payment_status='failed'` + Tracy + Jake alert. Card declines surface via `payment_intent.payment_failed`; **ACH first-payment bounces surface via `checkout.session.async_payment_failed`** (added 2026-06-15) — both now handled.

**Receipt email** (`SPECIALIST_bg_receipt`, fired on clearance) now carries the **DD Checklist** + **Request Help** buttons, the process image, and the **revenue-share proposal (Exhibit A)** with **Happy** / **Further Questions** buttons. `bg-receipt.ts` mints `ddc_token` + `rev_share_final_token`.

**Background Check section:** admin **Mark as sent** (Core→Checkr / Max→Scherzer) → **Passed** / **Failed** (Failed stops the onboarding).

**Due Diligence Checklist (review loop):**
- Specialist fills the **public DDC form** `/specialist-ddc?token=<ddc_token>` (`SpecialistDdcPage.jsx`, reworked 2026-06-04): four sections (Materials / Credentials & Experience / **Compliance & Risk — only shown to tax specialists** / Client Experience), each question in its own card. Input types: **file-only** (headshot, licenses, E&O, PLR, TOL, independent legal DD), **text-only** (bio), **file+text** (case studies, disciplinary history, audit support, exit strategies, sample materials). PLR/TOL/legal-DD are 3 separate fields under a "Provide one or more of the following:" subheading. Files go to the **private `specialist-dd-materials`** bucket via signed upload URLs (gotcha #69). **Nothing is required** (Tracy denies + re-sends if incomplete). **Save progress + resume** (token stays valid), then **Submit for review**.
- **Request Help** → `/specialist-ddc-help` → non-dismissible Tracy notif → admin "Help received" bullet.
- Admin reviews via the **"View answers"** expander (regrouped 2026-06-04 to mirror the form's sections + question order; all text answers + signed-URL file downloads via `ddcdownload`), then **Approved** (→ `SPECIALIST_ddc_approved`, now written; calls `maybeAdvanceStage3`) or **Denied** (reason → `SPECIALIST_ddc_edits` email w/ resubmit link, clears `ddc_submitted_at`). Loops until approved; every round is a **paper-trail** bullet (`ddc_review_log`, gotcha #71).

**Final Revenue Share Proposal section:** an automated step records the specialist's email response (Happy / Further Questions). Happy → done (drafts `SPECIALIST_revshare_complete`). Further Questions → Tracy **Confirm** (locks as-is) or **Edit** (edited text → `rev_share_final_text`, original `rev_share_prepared` preserved — gotcha #70); both also draft `SPECIALIST_revshare_complete` and call `maybeAdvanceStage3`. "View final proposal" only appears once finalized; Stage 2's "View proposal" shows Original + Edited.

## Stage 3 — completion emails (2026-06-04)

When each of the three Stage-3 items completes, the specialist gets a confirmation email via the shared helper `utils/specialist-stage3-emails.ts` → `sendStage3CompletionEmail`: **`SPECIALIST_bg_passed`** (id 89, on "Passed"), **`SPECIALIST_ddc_approved`** (id 87, on "DD Checklist Approved"), **`SPECIALIST_revshare_complete`** (id 90, on Happy / Confirm / Edit). Each email shows a **blue "Step 3 progress" card** listing all three items (green done-pill Passed/Complete/Finalized, orange Pending) plus either a "we have everything … bring your case forward for executive approval" line (all done) or a "we'll be in touch as the remaining items are completed" line. Idempotent per item via `<item>_complete_emailed` stage-3 progress markers.

## Stage 4 — Contract & Details: final executive approval (2026-06-04)

On entering Stage 4 (the all-three gate met), `maybeAdvanceStage3` fires a non-dismissible **reviewer-notes reminder** to Tracy. **The tax-risk notes are NOT required at Stage 4 (2026-06-10)** — for tax specialists they are supplied once, before the **Stage-2** vote; Stage 4 has no tax-risk input, no tax-risk vote-gate, and no tax-risk section in the exec "View details". (A separate Stage-4 tax-risk *reminder* bell does fire for tax specialists, `SPECIALIST_stage4_tax_risk_notes` — to Tracy since 2026-07-27.) The Stage 4 admin section then mirrors Stage 2 (minus the tax-risk notes):
1. **Reviewer notes** — Tracy only (general), saved as stage-4 `tracy_general_notes`; the reminder clears on save. Voting opens on the general notes alone (`save-progress.ts` `taxNotesRequired = isTax && stage === 2` — renamed from `timRequired` 2026-07-27).
2. **Final executive approval** — the same account-scoped 2-round voting via `automation_SPECIALIST_execvote` with `stage=4` (values redacted in `load.ts` until both vote that round). A **"View details"** dropdown shows the background check (Passed + Core/Max), the submitted DD checklist (text + file downloads), the final revenue share proposal, and **Tracy's general notes** (no tax-risk section).
3. **Outcomes:** both-Denied → `SPECIALIST_denied` + `status='stopped'`; **both-Approved auto-sends the Specialist Onboarding Agreement** — no button (see the sign/pay flow below).

### Stage 4 sign / pay / recurring-license (2026-06-05)

Both-exec-Approved → `exec-vote.ts` `applyApproval(4)` chains `automation_SPECIALIST_sendagreement` (**backgrounded via `EdgeRuntime.waitUntil`** so the voting click returns fast; idempotent on `agreement_sent_at`). Chain:
1. **`agreement-send.ts`** — fills `agreement_templates` id=12 HTML (`[PROPOSED_REVENUE_SHARE]` ← `rev_share_final_text` || the Stage-2 `rev_share_prepared` notes; also `[SPECIALIST_NAME]`/`[SPECIALIST_EMAIL]`), renders HTML→PDF (html2pdf), sends **coordinate-based BoldSign** (field_map: `specialist_address` p1; `specialist_signature`/`print_name`/`date` + `ert_signature`/`date` p7; signer 1 specialist, 2 Anton; BrandId per-account, gotcha #79), drafts `SPECIALIST_agreement_sent`, stamps `agreement_sent_at` + `agreement_sent` progress.
2. **`boldsign-webhook` specialist branch** (keyed `lic_boldsign_document_id`): specialist signed → `agreement_signed_by_specialist_at` + chain **`agreement-ceo-countersign.ts`** (drafts `SPECIALIST_ceo_countersign` w/ Anton's embedded link); Completed → `agreement_signed_by_ceo_at` + chain **`license-stripe-customer.ts`** (reuses `bg_stripe_customer_id` as `lic_stripe_customer_id`, mints `lic_checkout_token`, chains **`license-payment-email.ts`** → `SPECIALIST_lic_payment` w/ `/specialist-pay?token=&kind=license`).
3. **`/specialist-pay?kind=license`** → `license-load-payment.ts` (returns $99/mo) + `license-checkout.ts` (**Stripe Checkout `mode=subscription`**, monthly $99 `price_data`, card grossed-up 2.9%+$0.30 / ACH flat, metadata `pipeline=SPECIALIST_ONBOARDING`/`payment_kind=license`).
4. **Stripe webhook** (`router/webhooks.ts`): `checkout.session.completed` + `invoice.paid`/`invoice.payment_succeeded`/`invoice_payment.paid` (2026-07-07, the last for newer Stripe API versions) all funnel the invoice-side work through a shared `processSpecialistLicenseInvoicePaid()` — whichever lands first captures the payment method (gotcha #77) + marks `payment_made` + chains **`license-confirmation-email.ts`** (`SPECIALIST_lic_confirmation|card`/`|ach`; **the handler ATOMICALLY claims `lic_confirmation_email_sent_at` before drafting** so the two near-simultaneous chains can't both send — gotcha #108, 2026-06-10). For CARD payments, `checkout.session.completed` ALSO drives the first invoice/receipt inline by expanding the subscription's `latest_invoice`, so it no longer waits on a separate invoice event; the per-invoice claim on `lic_last_invoice_id` runs through a `SECURITY DEFINER` RPC (`claim_specialist_license_invoice`, committed as migration `20260708130000_…_rpc.sql` — gotcha #196), not a plain table update (gotcha #188); since v561 (2026-07-08) the processor also SKIPS an invoice whose event names a subscription other than the stored `lic_subscription_id` (the license reuses the bg Stripe customer — a foreign-subscription invoice must not be claimed as a license payment). First invoice → **`license-invoice-receipt.ts`** (`INV-SPECLIC`/`REC-SPECLIC` PDFs to Drive — license **reuses the bg invoice/receipt HTML generators**; `SPECIALIST_lic_invoicereceipt` email id 96 — **now written**, `[FIRST_NAME]`/`[AMOUNT]`/`[MONTH_LABEL]` substituted) + advance **Stage 4 → Stage 5** + **raise the two Tracy action-required notifs** (Skool + Create-specialist; see the Tracy ladder under Stage 5); recurring monthly invoice → monthly invoice/receipt; `invoice.payment_failed` → `lic_payment_status='failed'` + Tracy dunning FYI.

The 7 Stage-4 AutoSteps (Agreement sent → … → Invoice/receipt sent) are driven by these progress markers (no date chip). State in 20 `specialist_onboarding` columns (`lic_*` + `agreement_*`; see [tables/specialists.md](../tables/specialists.md)). **⚠️ `invoice.paid` + `invoice.payment_failed` must be enabled on the Stripe webhook endpoint** (sandbox done; **live pending** — gotcha #75). **Stage 5 (going live) is now automated** — see the next section.

## Stage 5 — Going Live (2026-06-10)

The first license `invoice.paid` advances Stage 4 → 5 and raises **two** non-dismissible Tracy action-required notifs. Stage 5 has two automated buttons + completion checkboxes.

**Send invite to VFO Skool** (replaced the old "Profile created" checkbox). `automation_SPECIALIST_skoolinvite` (AUTH): admin pastes the **Skool invite link** → drafts `SPECIALIST_skool_invite` (id 149), the Step-4-completed / Go-Live email with the inline `onboarding-process.png` (from `specialist-onboarding-assets`) and a `[SKOOL_LINK]` button. Marks `skool_invite` progress + clears the "Send VFO Skool invite" Tracy notif. **"Introduction post made"** is a checkbox (`intro_post`); **"Additional team members added"** is an optional checkbox (`team_members_added`) + a names box.

**Create the VFO Specialist** ("Add to Showroom & Send Specialist Login" button). `automation_SPECIALIST_createspecialist` (AUTH):
- Builds an `experts` directory row **FROM THE SIF** (question-to-question mapping): `sif_data.strategy_expertise`→`D&B_strategy_expertise`, `client_requirements`→`D&B_client_requirements`, `investment_cost`→`D&B_investment_cost`, `ideal_client`→`D&B_ideal_client`, `benefits_summary`→`D&B_summary_benefits`, `getting_started`→`D&B_getting_started`, `process_steps`→`D&B_professional_process`, `competitive_advantage`→`D&B_competitive_advantage`, `cutoff_date`→`D&B_cutoff_date`. The 4 `D&B_audit_risk_*` columns are written **only when `sif_data.is_tax_specialist='Yes'`**. `background_check` = `specialist_onboarding.background_check_type` (Core/Max).
- **Auto-filled (2026-06-10) so the admin no longer types them:** `short_bio` = `sif_data.strategy_expertise`; `long_bio` = the DD-checklist **Professional Bio** (`ddc_data.bio`); `D&B_revenue_share` = the finalized proposal (`rev_share_final_text` || the Stage-2 `rev_share_prepared` notes). All three stay editable in the Edit-Specialist form afterwards.
- **`experts.id` is assigned max+1** (the table has no sequence default). Idempotent on `expert_id` (re-clicks are no-ops).
- Copies the DD-checklist files into `specialist-documents/<expert_id>/`.
- Writes back `specialist_onboarding.expert_id` + `expert_created_at`; marks `added_to_showroom` progress.
- Chains the login-setup email (see below).
- **SPECIALISTS ARE NOT MEMBERS** — it writes `experts` + `specialist_logins` only (no `members` row).
- Clears the "Create the VFO Specialist" Tracy notif + raises **"Add headshot (white background)"** only (bios are auto-filled — the bios notif/checkbox were removed 2026-06-10).

**Login-setup (specialist portal)** — mirrors advisor/accountant. `automation_SPECIALIST_loginsetupemail` (chained, template `SPECIALIST_login_setup` id 150, link `/member-setup?token=`), then the public `automation_SPECIALIST_loadloginsetup` + `automation_SPECIALIST_submitloginsetup` write the passcode to **`specialist_logins`** (NOT `member_logins`). The shared `/member-setup` page falls through advisor → accountant → specialist and **redirects specialists to `/specialist/login`**. New `specialist_onboarding` columns: `login_setup_token` / `_token_expires_at` / `_email_sent_at` / `_completed_at`.

**Specialist portal** — a 4th login type (role `specialist`). A single **Vault** tab (general docs only) on the private **`specialist-documents`** bucket namespaced by `expert_id`. Handlers `specialist_vault_list` / `_upload_url` / `_download` / `_delete` are scoped to `auth.callerSpecialistId`. Routes `/specialist/login`, `/specialist`.

**Admin vault view** — Search Specialists → click a specialist → tabs **Edit Specialist | Vault**. The Vault tab uses admin-only handlers `specialist_vault_admin_list` / `_admin_download` / `_admin_upload_url` / `_admin_delete` (by `expert_id`) — admins can **view, add, and remove** (upload/delete added 2026-06-10). A `?expert=<id>` deep-link auto-opens a specialist (with a changing `&_n=` nonce so re-clicks always re-fire — gotcha #107).

**Stage-5 completion gate + Tracy ladder.** The Create button does **NOT** complete the onboarding — completion (`status='completed'`) requires the stage-5 checkbox `headshot_added` (gated in `save-progress.ts`; **the `bios_added` checkbox was removed 2026-06-10** now that bios auto-fill on go-live). Tracy (`tnmiller@elitert.com`) action-required ladder (all non-dismissible):
- first invoice → **"Send VFO Skool invite"** + **"Create the VFO Specialist & send login"**;
- `skoolinvite` clears the first;
- `createspecialist` clears the second + raises **"Add headshot (white background)"** (no bios action);
- the headshot checkbox clears its notif and completes the onboarding.

Stage-5 progress task_keys: `skool_invite`, `intro_post`, `team_members_added`, `added_to_showroom`, `headshot_added` (freeform keys in `specialist_onboarding_progress`; `bios_added` retired 2026-06-10).

## Reminder sweep (`specialist-sweep-daily`, 14:20 UTC Mon–Fri)

`automation_SPECIALIST_sweep` fires a **2-business-day reminder email** then a **4-business-day Tracy FYI** for **7 stalls** (plus **tier 7b**, the licence-CONTINUATION stall — a separate flow, see [specialist-license-continuation.md](specialist-license-continuation.md); it reuses tier 7's rule keys and guard columns and leaves tier 7 untouched): SIF not submitted, exec hasn't voted R1/R2 (emails only the missing exec(s)), Core/Max/Questions not chosen, **DD checklist not submitted**, **final rev-share unanswered**, **agreement not signed** (signature reminder fetches a fresh BoldSign link), **license payment not made**. (The old per-meeting rev-share stall was removed.) Timer-guard columns + bases: gotcha #66. NO 14-day auto-decline.

**Every tier counts BUSINESS DAYS as of 2026-08-14** (Mon–Fri UTC, **no holiday calendar**). The configured `notification_rules.delay_days` are unchanged as numbers — 2 and 4 here — but the sweep resolves each cutoff through `businessDelayCutoffIso()` in [`utils/notify.ts`](C:/vfo-edge-functions/supabase/functions/vfo-admin-api/utils/notify.ts), so a specialist who goes quiet on a Thursday is chased the following week rather than over the weekend. The 14 tiers all run through one shared wrapper, and the `daysTxt` helper that renders the delay into the email/bell body now emits *"N business day(s)"*.

## Notifications

To **Tracy** (`tnmiller@elitert.com` — who owns BOTH note prompts since 2026-07-27), and **Anton/Paul** for their specific steps; `pipeline='SPECIALIST_ONBOARDING'`, link `/admin?tab=specialists&section=specialist_onboarding&onboarding=<id>` (gotcha #61). **Action-required (non-dismissible, clear on the action):** reviewer-notes-needed (Tracy ×2 — general + tax-risk, cleared by exact title), vote-needed (Anton/Paul R1), second-decision-needed (R2), re-vote (split), further-questions (Tracy). **FYI (dismissible):** SIF submitted, rev-share response, voting completed (R1/R2), payment cleared, payment failed, all 4-business-day reminder escalations. (Full map: see the session notes.)

## Templates & assets

- `email_templates` (pipeline `SPECIALIST_ONBOARDING`, all editable incl. CC/BCC): `SPECIALIST_yes`, `SPECIALIST_no`, `SPECIALIST_step2_progress`, `SPECIALIST_denied`, `SPECIALIST_step3`, `SPECIALIST_step3_proceed`, `SPECIALIST_bg_receipt` (id 80, now **written** — DD checklist + Exhibit A), `SPECIALIST_bg_confirmation|card`, `SPECIALIST_bg_confirmation|ach`, `SPECIALIST_ddc_edits` (id 88, Denied reason + resubmit link), `SPECIALIST_ddc_approved` (id 87, **now written**), the two new Stage-3 completion emails `SPECIALIST_bg_passed` (id 89) + `SPECIALIST_revshare_complete` (id 90) (gotcha #73), the reminder templates `SPECIALIST_sif_reminder`/`_vote_reminder`/`_choice_reminder` + **(2026-06-05)** `_ddc_reminder`/`_revfinal_reminder`/`_signature_reminder`/`_licpayment_reminder` (97–100), and the **Stage-4 set**: `SPECIALIST_agreement_sent` (91), `SPECIALIST_ceo_countersign` (92), `SPECIALIST_lic_payment` (93), `SPECIALIST_lic_confirmation|card` (94) / `|ach` (95), `SPECIALIST_lic_invoicereceipt` (96, **now written** — `[FIRST_NAME]`/`[AMOUNT]`/`[MONTH_LABEL]`), and the **Stage-5 / portal set (2026-06-10)**: `SPECIALIST_skool_invite` (149, inline `onboarding-process.png` + `[SKOOL_LINK]`), `SPECIALIST_login_setup` (150, `/member-setup?token=` link). **Removed:** `SPECIALIST_revshare_reminder` (dead per-meeting reminder). All specialist emails end with the shared `VFO_SIGNATURE` footer (*"VFO Services - Proactive Coordinator Team"* since 2026-08-20).
- `agreement_templates` id=12 (`Specialist`/`Single`) holds the agreement `html_body` (placeholders `[SPECIALIST_NAME]`/`[SPECIALIST_EMAIL]`/`[PROPOSED_REVENUE_SHARE]`/**`[STRATEGY_EXPERTISE]`** — the last fills the WHEREAS recital line from `sif_data.strategy_expertise`, on a single-line/clip-overflow span so it can't wrap and shift the page-7 signature coords; added 2026-06-10) + a populated **`field_map`** (BoldSign coords) + **`boldsign_template_id`** `a04ead86-…`. The Stage 1 email attaches the static reference PDF; the **BoldSign signing flow renders the HTML→PDF fresh** per-specialist (gotcha #79).
- Public bucket `specialist-onboarding-assets`: `onboarding-process.png`, `VFO-Specialist-Agreement.pdf`, `revenue_share_examples.pdf`, `VFO-Specialist-Onboarding-Presentation.pdf` (all three PDFs attached to the Stage 1 email). **Private** bucket `specialist-dd-materials` for DD checklist uploads (gotcha #69). **Private** bucket `specialist-documents` (2026-06-10) for the go-live specialist Vault, namespaced `<expert_id>/<rand>_<filename>` — the create-specialist step copies the DD files here.
- Receipt/invoice PDFs are html2pdf-generated (`utils/specialist-html-templates.ts`) and archived to Drive **`VFO Specialist Onboarding/<Name> - Specialist`** (parent `13eFhVvCgABtRgRbg0l4s2sUilOzMjrWb`).

## Frontend

`components/admin/SpecialistOnboarding.jsx` (Stages 1–5 tracker, Stage 5 automated 2026-06-10; **2026-08-26 the LIST gained collapsible Completed / Stopped sections** — it had been a flat list of every row — so in-progress onboardings stay loose at the top and the two terminal buckets collapse away, matching the advisor / accountant lists' three-bucket shape) + `components/admin/SpecialistAutomationPanel.jsx` (Automation tab → "Specialist Onboarding"); pages `SpecialistSifPage.jsx` (`/specialist-sif`, with the tax-specialist branch; **Strategy/Expertise is a single-line input capped at 85 chars with an `n/85` counter — 2026-06-10**, mirrored by a server-side 85-char truncate in `submit-sif.ts` so it can never wrap in the agreement), `SpecialistPayPage.jsx` (`/specialist-pay` — `&type=core|max` one-time background check **or** `&kind=license` for the $99/mo subscription, added 2026-06-05), `SpecialistQuestionsPage.jsx` (`/specialist-questions`), and (2026-06-04) `SpecialistDdcPage.jsx` (`/specialist-ddc`, file-upload DD form), `SpecialistDdcHelpPage.jsx` (`/specialist-ddc-help`), `SpecialistRevShareFinalPage.jsx` (`/specialist-revshare-final`). **Removed:** `SpecialistRevShareDecidePage.jsx` + `/specialist-revshare-decide` (orphaned after per-meeting rev-share dropped). `EmailTemplatesPanel.jsx` Specialist section; `AdminPortal.jsx` deep-link + automation section; skeletons `SpecialistOnboardingListSkeleton`/`SpecialistOnboardingDetailSkeleton`.
- **Specialist portal (2026-06-10):** a 4th login type (role `specialist`) — login + Vault pages at `/specialist/login` and `/specialist`; the shared `/member-setup` page falls through advisor → accountant → specialist and redirects specialists to `/specialist/login`. Admin **Search Specialists** → click → **Edit Specialist | Vault** tabs (Vault via `specialist_vault_admin_list`/`_admin_download`; `?expert=<id>` deep-link auto-opens).

## Not yet built / follow-ups

- ~~**Stage 4 sign/pay flow**~~ **DONE (2026-06-05)** — agreement auto-send → BoldSign → CEO countersign → $99/mo Stripe subscription → confirmation → invoice/receipt → Stage 4→5 (see the Stage-4 section above). The `SPECIALIST_lic_invoicereceipt` body is **now written** (2026-06-10). **Remaining for go-live:** (a) `npm run deploy` the frontend (the `/specialist-pay?kind=license` page + Stage-4/5 + portal UI live only on the dev server); (b) add `invoice.paid` + `invoice.payment_failed` to the **live** Stripe webhook; (c) flip SPECIALIST sandbox→live; (d) the invoice/receipt email shows `$99` even for card payers actually charged $102.27 (gross-up) — wording fix deferred.
- ~~**Stage 5 (going live)**~~ **DONE (2026-06-10)** — automated: first invoice raises the Tracy ladder → Send VFO Skool invite → Create the VFO Specialist (`experts` row built FROM THE SIF + DD files copied to `specialist-documents/<id>/` + login email) → specialist portal (role `specialist`, single Vault tab). Completion gated on `headshot_added` + `bios_added`. Migration `specialist_login_and_expert_link`.
- ~~`SPECIALIST_ddc_approved` TBD~~ **DONE** — body written this session (gotcha #72 resolved); `SPECIALIST_bg_receipt` (id 80) also written.
- ~~Stage 3 gate question~~ **RESOLVED** — Stage 3 → Stage 4 now requires all three (bg Passed + DD Approved + rev finalized) via `maybeAdvanceStage3`.
- ~~**Specialist pipeline is in sandbox**~~ **LIVE** — `select pipeline, sandbox_mode from pipeline_sandbox_config` shows SPECIALIST_ONBOARDING `false` (re-verified 2026-08-24, all 8 pipelines live). Borrow the toggle for a test run and flip it back in the SAME sitting (#430).
- ~~Frontend production deploy required~~ **DONE** — the whole Specialist UI is now deployed/live. (Code is deployed but **not yet committed** in git.)
- Card declines mid-Checkout don't webhook; **ACH first-payment bounces now do** (`checkout.session.async_payment_failed`, 2026-06-15 → `bg_payment_status='failed'` + Jake alert). License lapse/cancel also alerts now (`customer.subscription.updated`/`deleted` → "consider revoking access").

## Cross-references

- License Fee Continuation (existing specialists): [specialist-license-continuation.md](specialist-license-continuation.md)
- Specialist tables: [../tables/specialists.md](../tables/specialists.md)
- Action catalog: [../architecture/05-api-action-catalog.md](../architecture/05-api-action-catalog.md)
