# UNIT 2 — PHASE 5e BRIEF — member notification bell; PF bells go to the member on Direct; two new PF bells; "Automated steps" label + chips

You are the implementer (Opus). Fable planned, Fable reviews. Read `unit2-phase0-brief.md` "Absolute rules". Edge worktree `C:\vfo-edge-functions\.claude\worktrees\vfo-session-setup-7559c8`, react worktree `C:\vfo-react\.claude\worktrees\vfo-session-setup-7559c8`; both clean. Do not commit/deploy/migrate/write SQL (migrations are FILES). Dev server running.

Read first: `docs/flows/notifications.md`, `docs/tables/notifications.md`, `utils/notify.ts`, `utils/notify-jake-failure.ts`, `utils/tax-notify.ts` (ALL of it: `PF_LOGIN_EMAILS`, `taxPfLoginEmail`, `taxPfRecipients`, `taxDecisionRecipients`, `insertTaxNotifications`), `utils/tax-review-bell.ts` (header), `actions/notifications/load.ts` + `mark-read.ts` (how a `tax_planner` session is scoped to its own email — #259), `constants/role-gates.ts` (`load_notifications` / `mark_notification_read` gates), `actions/auth/member-login.ts` + `middleware/auth.ts` (what a member session carries: `callerMemberNumber`, `session.email`), `src/components/NotificationBell.jsx` + its mounts in `AdminPortal.jsx` / `TaxPlannerPortal.jsx`, `src/pages/MemberPortal.jsx` header, `actions/tax/ready-for-tax3.ts` (where `tax3_meeting_date` / `tax3_meeting_time` / `tax3_meeting_tz` are written), the 5-minute reminder sweep (cron `reminder-sweep-5min` → find its action in `router/dispatch.ts` and the handler), `actions/tax/save-task.ts` (generic step save; "ROI Presentation" is a name-matched dropdown), `actions/tax/decision.ts`, `TaxPrioritiesTab.jsx` (`OwnerChip`, `OWNER_CHIP_COLORS`, `ownerLabel`, `stepOwner`, `TASK_DISPLAY_LABELS`, the two "AI PC Admin" cards ~L3454 and ~L3984, "Waiting for details"), `src/components/admin/map1/ClientTrackViewV2.jsx` + `pip/PipMeetingsTab.jsx` + `regular/*` (their "AI PC Admin" cards), `docs/GOTCHAS.md` #259 #365 #176 #313 #324 #413 #411 #448 #455 #382.

## Jake's decisions (2026-09-21, final)
1. **Members get a notification bell** in the member portal, scoped to their own login email only (never `admin` / `all` broadcasts).
2. **On a Direct plan, every bell addressed to the PF goes to the MEMBER** (the PF) instead of a VFOS login; on a classic plan it goes to the assigned PF's login as today. Same titles, same words, same clear sites. Bells addressed to Tray/Tracy that are really "the PF's next step" on Direct — "Book the ROI meeting for the client." (4a), the Red Light instruction (4c), the deposit-refund/stop FYI (4d), "Download the ROI presentation" (5), "Send the detailed tax plan meeting confirmation email" (6) — go to the member on Direct. Tracy keeps her copies where she has them today on classic plans (do not change classic routing at all).
3. **Two NEW PF bells on EVERY plan** (classic → assigned PF; Direct → member):
   - `TAX_roi_meeting_confirm_needed` — title `Confirm the ROI meeting happened for [Client Name]`, message `The ROI meeting for <client> was booked for <date> <time> <tz>. Please confirm it took place on the "ROI Presentation" step.` — fired by the **5-minute reminder sweep** once `tax3_meeting_date` + `tax3_meeting_time` (+ tz) is in the past, once per plan (latch column `roi_confirm_bell_sent_at`), only while the "ROI Presentation" step is unanswered and the plan is live and not ROI-skipped. Action-required, `dedupe:"unread"`. **Cleared** when "ROI Presentation" is answered (any value) in `save-task.ts`, or the plan closes.
   - `TAX_client_decision_needed` — title `Record the client's tax planning decision for [Client Name]`, message `The ROI presentation for <client> is confirmed. Please record the client's decision on the "Client tax planning decision" step.` — fired in `save-task.ts` when "ROI Presentation" is saved (any value) and the decision is still empty. Action-required, `dedupe:"unread"`. **Cleared** when any decision is recorded (`decision.ts` — Yes / No / Undecided; reuse the existing clear the `TAX_roi_skipped_decision_needed` bell has there, which matches by title prefix — add this title prefix beside it).
   Both seeded in `notification_rules` (area Tax, `action_required true`, `default_recipients ["ASSIGNED_PF"]`, sort 271 / 272), migration `20260921140000_tax_pf_roi_decision_bells.sql` (rules + the latch column `client_tax_plans.roi_confirm_bell_sent_at timestamptz`; DDL + data in one file is fine here, no policy change).
4. **"AI PC Admin" is displayed as "Automated steps" everywhere in the system** (display label only — the stored `program_client_tasks.name` and every `=== 'AI PC Admin'` match stay untouched, #382). Its owner chip reads **Automated** (grey). Its "Waiting for details" pill becomes a gate-style hint (circle-slash + `lockedHintStyle`) reading `Starts automatically after the "<previous step display name>" step` — derive the previous step's name from the phase's task order, or hard-map per card if simpler (Tax 3: Client tax planning decision; Tax 5b: Implementation decision; MAP 1 / PIP: whatever precedes them).
5. **Chip colours:** VFOS → green (`rgba(27,146,84,0.12)` / `#1b9254`); Client → purple (`rgba(111,66,193,0.12)` / `#6f42c1`); Tax Team stays orange; Member stays blue; Automated grey (`var(--vfo-tint)` / `var(--vfo-muted)`).

## Build

### A. Member bell (backend)
1. `actions/notifications/load.ts` + `mark-read.ts`: a `member` caller is scoped EXACTLY like a `tax_planner` (#259): rows whose `recipient` equals the session's login email (lower-cased), never `admin` / `all`; mark-read only on own rows. Find what a member session exposes as its email (`session.email` from `member_logins`?) and use that — report it. Both actions must be reachable by a member: check the gate lists (they are on the planner allowlist; members are not deny-by-default, so confirm neither is ADMIN_ONLY / TAB-gated, and if either is, move it to in-handler scoping rather than widening a gate — #490).
2. **The address a member bell is written to = the member's LOGIN email** (whatever `member_logins` stores; if it can differ from `members.email`, resolve the login email — report which you used and why).

### B. Member bell (frontend)
Mount `NotificationBell` in `MemberPortal.jsx`'s header exactly as `TaxPlannerPortal.jsx` mounts it (own-rows only; "View all" hidden unless the member has a page for it — hide it). Row click navigates to the bell's `link`; for member bells the link must be `/member/client/<client_id>` (see C.3). Nothing else in the member portal changes.

### C. PF resolution goes through the client row (Direct → member)
1. `utils/tax-notify.ts`: add `resolveTaxPf(sb, client: { assigned_pf?, pf_member_number? })` → `{ loginEmail: string | null, isMember: boolean, memberNumber?: string }` — `pf_member_number` set → the member's login email; else `taxPfLoginEmail(assigned_pf)`. Then `taxPfRecipientsFor(sb, client)` and `taxDecisionRecipientsFor(sb, client, plannerEmails)` — async twins of the existing two with the same fallbacks (Tracy). Keep the sync originals for any caller that has only a name.
2. Rewrite EVERY tax bell site that resolves the PF (`taxPfRecipients(` / `taxPfLoginEmail(` / `taxDecisionRecipients(` / the `ASSIGNED_PF` dynamic token) to the client-row twin, widening each site's `.select(...)` with `pf_member_number` (#448 audit table: file → select line → added). The phase-2 census listed the bell-only selects deliberately left alone: `extra-meeting.ts:66`, `final-decision.ts:146`, `postreview-client-decision.ts:130`, `refund.ts:202`, `revshare-sweep.ts:417/494/592/668/706`, `utils/tax-amended-invoice-chain.ts:72`, plus `save-task.ts`, `save-assess-form.ts`, `skip-roi-meeting.ts`, `confirmation-email.ts`, `deposit-refund.ts`, `stop-no-deposit.ts`, `implement-final-decision.ts`, `implementation-receipt.ts`, `utils/assess-vault-stamp.ts`, `utils/vault-planner-notify.ts`, `actions/vault/member-client-vault-shared.ts`, `actions/agreements/declined.ts`. Grep to be sure; report the full list.
3. **Links:** `notifyByRule` / `insertTaxNotifications` accept a per-recipient `links` map. For a member recipient set `links[memberEmail] = /member/client/${client_id}` (the member portal's client page; the tax tab is the default there — check `ClientDetail.jsx`'s member route and add `?tab=tax` if the member route honours it). Admin recipients keep the admin link.
4. **Decision 2's five Tray/Tracy bells on Direct** — 4a and 4c (`TAX_planner_review_complete` in `save-task.ts`, `defaultRecipients: ["tvaldes@elitert.com"]` today + the rule's recipients), 4d (`TAX_deposit_refund_issued` / `TAX_plan_stopped_no_deposit`), 5 (`TAX_assess_completed_pf`), 6 (`TAX_planner_hlm_ready` / `TAX_roi_skipped_hlm_ready`): on a Direct plan (`client.pf_member_number` set) the recipient list becomes `[memberLoginEmail]` ONLY, via `dynamic`/`defaultRecipients`; classic plans byte-identical. Report each site's hunk.
5. **Clear sites** are title-matched and recipient-agnostic already (verify `clearTaxReviewBells` and the others do not filter on recipient; if any does, make it recipient-agnostic).

### D. The two new bells (backend)
1. Migration `20260921140000_tax_pf_roi_decision_bells.sql`: `client_tax_plans.roi_confirm_bell_sent_at timestamptz` + the two `notification_rules` rows (copy the shape of `TAX_plan_stopped_no_deposit` but `action_required true`).
2. Sweep: in the 5-minute reminder sweep handler, a new isolated pass: select live program-1 + program-4 plans with `tax3_meeting_date` not null, `roi_meeting_skipped_at` null, `roi_confirm_bell_sent_at` null, whose meeting instant (date + time in tz; if no time, end of that day; if no tz, ET) is `< now()`, and whose "ROI Presentation" progress row is empty → fire `TAX_roi_meeting_confirm_needed` to `taxPfRecipientsFor(client)` with the member link on Direct, stamp the latch. Per-row try/catch. Report the time-zone handling you used (there is a tz helper somewhere for the reminder ladders — reuse it).
3. `save-task.ts`: when the saved task is named "ROI Presentation" and `priorStatus !== status` and status non-empty → clear `Confirm the ROI meeting happened for%` for that client; then if the plan's `tax_decision` is empty → fire `TAX_client_decision_needed` to the PF/member. Also clear `Record the client's tax planning decision for%` in `decision.ts` on any decision (beside the existing roi-skipped clear).
4. Both bells: `dedupe:"unread"`, `dismissible:false`, `pipeline:"TAX"`, `client_id`.

### E. "Automated steps" + chips (frontend, all repos' surfaces)
1. `TaxPrioritiesTab.jsx`: `TASK_DISPLAY_LABELS['AI PC Admin'] = 'Automated steps'` (both cards use `taskLabel(task)` — verify; the Tax 5b card too). `stepOwner` returns `'auto'` for `AI PC Admin`; `OwnerChip` renders **Automated** (grey) for it; `ownerLabel('auto')` → `'Automated'`. Remove the `so === 'auto' || name === 'AI PC Admin' → null` short-circuit only for the AI PC Admin cards (other `auto` rows stay chip-less).
2. "Waiting for details" pill (grep it — both tax cards) → the gate-hint markup (`LockedIcon` + `lockedHintStyle`) with `Starts automatically after the "<prev step label>" step`; once the cascade has started, the card renders as today.
3. MAP 1 (`ClientTrackViewV2.jsx`), PIP (`PipMeetingsTab.jsx`), Regular (`regular/*`), and any other surface printing the literal "AI PC Admin" (grep `src/` for it) → display "Automated steps". Client Overview / planner portal get their labels from the backend: in `utils/tax-plan-steps.ts` and the MAP1/PIP/Regular overview engines, if the label "AI PC Admin" is emitted, map it to "Automated steps" at the emit (a display map, not a rename). Report every site.
4. Colours per decision 5 in `OWNER_CHIP_COLORS` (and the same map if the planner portal has its own copy — grep).

### F. Probe + docs
- Probe: `load_notifications` / `mark_notification_read` with the member token → 200 and every returned row's `recipient` equals the member's email (add a check that fails if any row has recipient `admin` or `all`).
- Docs: `docs/flows/notifications.md` (member bell + Direct routing rule), `docs/tables/notifications.md` (two rules + the latch column), `docs/flows/tax-planning.md` (the two bells in the inventory + the chain; "Automated steps" display name), `docs/flows/tax-intake.md` cross-ref, `docs/plans/direct-tax-planning/README.md` decision 28 + remove the backlog paragraph, `docs/architecture/04-auth-and-sessions.md` (member bell scoping).

### G. Verify
`deno check` 0; action count (report; expect 533 unchanged unless you had to add an action — say why if so); `npm run build` exit 0; the #448 select-audit table; the full list of re-addressed bell sites with hunks; the sweep pass text; the tz handling; the member-bell scoping hunks; every "AI PC Admin" display site. Then a click-through for Jake: on plan 207 (Direct, ROI booked for 9/23 11:55 ET — to test the sweep without waiting, Fable will back-date `tax3_meeting_date` by SQL), the member sees the "Confirm the ROI meeting happened" bell in their portal, answers ROI Presentation → bell clears and "Record the client's tax planning decision" appears → records Yes → clears; on a classic plan the same two bells reach the assigned PF's admin bell.
