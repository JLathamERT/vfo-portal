# UNIT 2 — PHASE 5f BRIEF — Amend fee = Tax Team on Direct; Client tax planning decision (+ Undecided pricing / extra meeting) = the allocated planner on Direct

You are the implementer (Opus). Fable planned, Fable reviews. Read `unit2-phase0-brief.md` "Absolute rules". Edge worktree `C:\vfo-edge-functions\.claude\worktrees\vfo-session-setup-7559c8`, react worktree `C:\vfo-react\.claude\worktrees\vfo-session-setup-7559c8`; both clean at edge `360974a` / react `01b4a9d`. Do not commit/deploy/migrate/write SQL. No migration is expected in this phase. Dev server is running on http://localhost:5174 (port 5173 belongs to another tree — ignore it).

Read first (in this order): `docs/plans/direct-tax-planning/README.md` §7 (the phase-5f scope paragraph is Jake's decision — this brief is how to build it), `constants/role-gates.ts` (`TAX_PLANNER_ALLOWED_ACTIONS` incl. the stale comment that says decision/pricing/extrameeting/sendagreement are "NO LONGER planner-callable", and `TAX_DIRECT_MEMBER_ACTIONS`), `middleware/auth.ts` L195–260 (gate order), `utils/tax-direct-member.ts` (whole file), `utils/tax-planner-ownership.ts` (`resolvePlannerGroupIds`, `denyIfNotPlannerPlan`), `router/dispatch.ts` L730–765 + L1060–1095, `actions/tax/decision.ts`, `pricing.ts`, `extra-meeting.ts`, `send-agreement.ts` (the four handlers; note each chains `automation_TAX_sendagreement` over HTTP off `body.__direct`), `actions/tax/save-task.ts` (`PLANNER_EDITABLE_TASK_NAMES`, `DIRECT_MEMBER_EDITABLE`, the planner per-step check ~L107, the `TAX_roi_skipped_decision_needed` fire ~L278, the `TAX_client_decision_needed` fire ~L215 — that one is the PATTERN for F below), `actions/tax/direct-save-task.ts`, `actions/tax/skip-roi-meeting.ts` ~L200–235, `utils/tax-planner-notify.ts notifyAllocatedPlanner`, `utils/tax-review-bell.ts` header (the prefix map), `scripts/probe-tax-intake-refusals.ps1`, and in react `src/components/admin/tax/TaxPrioritiesTab.jsx`: `PLANNER_EDITABLE_TASK_NAMES` + `isPlannerEditable` (~L407–430), `DIRECT_EDITABLE_TASKS` / `isDirectEditable` / `DIRECT_ACTION_MAP` / `actFor` (~L442–494), `STEP_OWNER` / `stepOwner` / `lockHintFor` (~L497–540), `isDirectPlanView` / `ownerLabel` / `stepName` / `renderTask` (~L3170–3310), `renderTaskInner`'s `memberLocked` (~L3320) and the `AmendFeeStep` mount, the "Client tax planning decision" card (~L3531–3595, note `TaxDecisionForm` calls `tax_save_task` FIRST and `automation_TAX_decision` SECOND, ~L1150), the Tax 3 "AI PC Admin" card (~L3595–3800: the `TaxPricingForm` mounts and the extra-meeting Yes/No buttons are gated on `!memberLocked` only), `src/pages/ClientDetail.jsx` L83–94 (`isMember` / `isPlanner` / `isDirect`). Gotchas: #257 #262 #385 #455 #467 #490 #511 #339 #411 #448.

## Jake's decisions (2026-09-21, final — README §7 "PHASE 5f")
1. **Amend fee + Amend implementation fee on a Direct plan = Tax Team only.** The member is locked out (chip "Tax Team", lock hint "Handled by the Tax Planning Team"). Classic plans unchanged: VFOS or planners, chip VFOS. Planners ALREADY hold `automation_TAX_amend_fee` + both step names, so the backend change is purely removing the member's path.
2. **"Client tax planning decision" on a Direct plan = the allocated Tax Planner** (either slot-holder of the group), including the two Undecided follow-ups the Tax 3 Automated-steps card hosts (pricing after the client's Yes; extra-meeting Yes/No). The member is locked out (chip "Tax Team"). **Classic stays VFOS** — a planner on a classic plan gets a 403 from the handler, so classic routing is byte-identical (Jake: it will be opened to tax teams later, NOT in this unit).
3. The `TAX_roi_skipped_decision_needed` bell ("Complete the Client tax planning decision for …") on a Direct plan goes to the allocated planner(s), exactly like `TAX_client_decision_needed` already does (phase 5e). Classic unchanged.
4. Nothing else about Direct changes. The `TaxDecisionForm` split-option quirk flagged in §7 stays flagged, not fixed.

## Build

### A. Gate lists — `constants/role-gates.ts`
1. `TAX_PLANNER_ALLOWED_ACTIONS`: ADD `automation_TAX_decision`, `automation_TAX_pricing`, `automation_TAX_extrameeting`, `automation_TAX_sendagreement` as their own commented block: planner-callable ONLY on a Direct plan — each handler refuses a planner caller on a non-Direct plan in-handler (`denyIfPlannerOnClassicPlan`, B.1) AFTER the group guard, so the allowlist entry is inert on every classic plan (#467: an entry is not a grant). `_sendagreement` is on the list because the three decision handlers chain to it over HTTP with the CALLER's token, and a planner's token re-enters this gate. Fix the older comment block above `tax_save_task` that lists these four as "NO LONGER planner-callable" — it must now say Direct-only, pointing at the new block.
2. `TAX_DIRECT_MEMBER_ACTIONS`: REMOVE `tax_direct_decision`, `tax_direct_pricing`, `tax_direct_extrameeting`, `tax_direct_amend_fee`, `tax_direct_sendagreement`. Add one sentence to the list comment: the decision, its two Undecided follow-ups and the agreement chain moved to the allocated planner on 2026-09-21 (phase 5f), and the two amend steps are the Tax Team's — none has a member twin.

### B. `utils/tax-direct-member.ts`
1. New export `denyIfPlannerOnClassicPlan(supabase, planId, body, json): Promise<Response | null>`: `resolvePlannerGroupIds(supabase, body)` → `null` means not a planner → return `null`; else if `await isDirectPlan(supabase, planId)` → `null`; else `json({ error: "VFO Services records this decision on this plan" }, 403)`. Header comment: the Direct-only widening of four planner actions (A.1); ordered AFTER `denyIfNotPlannerPlan` at every call site so a planner outside the group still reads the plain `Forbidden`.
2. REMOVE `body.__direct = true` from BOTH wrappers and the comment explaining it. After D there is no member path into the three chaining handlers, so the marker has no consumer. `grep -rn __direct` across the edge function must return 0.

### C. The four handlers
`actions/tax/decision.ts`, `pricing.ts`, `extra-meeting.ts`, `send-agreement.ts`: directly after the existing `denyIfNotPlannerPlan` line, add
```ts
const classic = await denyIfPlannerOnClassicPlan(supabase, tax_plan_id, body, json);
if (classic) return classic;
```
(before every validation and every write). In the three chain calls replace `body.__direct ? "tax_direct_sendagreement" : "automation_TAX_sendagreement"` with the literal `"automation_TAX_sendagreement"`. Do NOT touch anything else in these files (the HTTP chain stays an HTTP chain).

### D. `router/dispatch.ts`
Remove the five `const direct… = directMemberHandler(…)` declarations (`directDecision`, `directPricing`, `directExtraMeeting`, `directSendAgreement`, `directAmendFee`) and their five `AUTH_HANDLERS` rows. Fix the block comment above the declarations: only `taxGeneratePresentation` is still adapted for its `auth` 4th parameter. Action count 533 → **528** (6 + 522). Leave every import that is still used by the admin rows.

### E. `actions/tax/save-task.ts` + `direct-save-task.ts`
1. `DIRECT_MEMBER_EDITABLE`: remove `enter_details` from `sentinelStatuses` AND `sentinelNames`, remove `tax_amend_fee` and `tax_amend_fee_tax5`. Update its comment (the decision and both amend steps are named as NOT the member's, with the date).
2. Planner per-step check (~L107): the decision step's ROW is written by `tax_save_task` from `TaxDecisionForm` BEFORE `automation_TAX_decision` runs, so a planner on a Direct plan must be able to save the step named `"Client tax planning decision"` (status `Completed - Yes|Undecided|No`, notes = the form JSON). Do NOT add the name to `PLANNER_EDITABLE_TASK_NAMES` (that list is unconditional and exported; a planner on a classic plan must still 403). Instead:
```ts
const DIRECT_PLANNER_TASK_NAME = "Client tax planning decision";
if (plannerGroupIds !== null && !PLANNER_EDITABLE_TASK_NAMES.includes(taskName)) {
  const directException = taskName === DIRECT_PLANNER_TASK_NAME && await isDirectPlan(supabase, tax_plan_id);
  if (!directException) return json({ error: "Tax planners cannot edit this step" }, 403);
}
```
with a comment naming phase 5f. `isDirectPlan` is exported from `utils/tax-direct-member.ts` (check for an import cycle: `tax-direct-member.ts` imports `client-ownership.ts` and, after B.1, `tax-planner-ownership.ts`; `save-task.ts` importing `tax-direct-member.ts` is fine — report if `deno check` says otherwise).
3. `direct-save-task.ts`: the `notes` carry-through is keyed on `sentinel === "enter_details"`, which is no longer an editable sentinel — the branch is dead. Remove it and `NOTES_MAX`, and the comment above it; rebuild the forwarded body without `notes`. Report this as a deliberate removal.

### F. `TAX_roi_skipped_decision_needed` → the planner on Direct
Two fire sites: `actions/tax/skip-roi-meeting.ts` (~L214, retainer_first) and `actions/tax/save-task.ts` (~L278, meeting_first). At each, mirror the phase-5e split used for `TAX_client_decision_needed` at save-task.ts ~L215: `if (cl?.pf_member_number)` → `notifyAllocatedPlanner(supabase, "TAX_roi_skipped_decision_needed", plan, { title: <same title>, message: <same message>, dedupe: "unread", dismissible: false })`; else the existing `notifyByRule` call unchanged. `notifyAllocatedPlanner` needs `plan.id`, `plan.client_id`, `plan.program_id`, `plan.tax_planner_id` and tolerates `tax_team_member_id === undefined` (it looks it up) — audit both sites' plan `.select(...)` and add `tax_planner_id` / `program_id` where missing (#448 table in the report). Titles unchanged, so `clearRoiSkippedDecisionBells` in `decision.ts` retires either vintage (#411). Update the `utils/tax-review-bell.ts` header entries for both `'Complete the Client tax planning decision for'` and `'Record the client's tax planning decision for'` — the latter currently says it is "the ONE PF bell that does not follow the member on Direct"; there are now two, both because the planner records the decision on Direct.

### G. Frontend — `src/components/admin/tax/TaxPrioritiesTab.jsx`
1. `DIRECT_EDITABLE_TASKS`: remove `enter_details`, `[AMEND_FEE_CODE]`, `[AMEND_FEE_TAX5_CODE]` from `sentinels`, and remove `namesInPhase['AI PC Admin']` (the Tax 3 card no longer hosts anything the member runs; keep the `namesInPhase` mechanism only if something else uses it — otherwise delete it and the branch in `isDirectEditable`). Update the contract comment. `DIRECT_ACTION_MAP`: remove the five entries for the retired twins.
2. Owner chips on Direct: `stepOwner(task, phase, direct = false)` — new first branch after the `AUTO_STEP_NAME` / `'auto'` checks: `if (direct && DIRECT_TEAM_SENTINELS.has(so)) return 'team'` with `const DIRECT_TEAM_SENTINELS = new Set(['enter_details', AMEND_FEE_CODE, AMEND_FEE_TAX5_CODE])` and a comment (phase 5f: on a Direct plan these three are the Tax Planning Team's — backend `DIRECT_MEMBER_EDITABLE` no longer lists them). EVERY call of `stepOwner(` inside `TaxPlanTrackView` passes `isDirectPlanView` as the third argument (grep — there are at least four: `stepName`, the two lock-wrapper `lockHintFor` calls, the `AmendFeeStep` `nameChip`; report the full list). `ownerLabel` needs no change ('team' → "Tax Team").
3. Planner editable on Direct: `isPlannerEditable(task, phase = null, direct = false)` = the existing name-set test OR `(direct && ((task.status_options === 'enter_details' && task.name === 'Client tax planning decision') || (task.name === 'AI PC Admin' && phase?.name === 'Tax 3 - ROI Meeting')))`. The one call in `renderTask` passes `(task, phase, isDirectPlanView)`. Comment: cross-repo contract with save-task.ts E.2 + role-gates A.1.
4. Inside the two cards a planner can now reach on Direct, hide what a planner must not click: in the decision card the `StepEmailsChip` beside the step name is UNCONDITIONAL today (it was unreachable behind the planner lock) — wrap it in `!(readOnly || plannerMode) &&` like its siblings (~L3815 pattern). Read the whole Tax 3 "AI PC Admin" card for any other admin-only control that is gated on neither `memberLocked` nor `plannerMode` and would become live for a planner (report each; the `sharedSteps` chips are already `(readOnly || plannerMode) ? null`). The `TaxPricingForm` mounts and the extra-meeting buttons must be reachable by the planner (they are gated on `!memberLocked`, which is false for a planner — confirm).
5. Member view on Direct after G.1: the decision step falls to its existing `memberLocked` arms ("Not started" / decision chip), the two amend steps render `AmendFeeStep readOnly`, and the Tax 3 Automated card is wrapped by the `directMode && !directEditable` lock. Confirm by reading, and confirm the lock hint for all three now reads "Handled by the Tax Planning Team" (via G.2).
6. `TaxDecisionForm`'s `act` prop: in plannerMode `act` is identity, so the planner's submit calls `tax_save_task` then `automation_TAX_decision` — no FE change needed; confirm.

### H. Probe — `scripts/probe-tax-intake-refusals.ps1`
Add optional `-PlannerEmail` / `-PlannerPasscode` (the planner-portal login; find the login action the portal uses) and `-PlannerClassicPlanId` (default 0 → SKIP the planner checks; a classic plan in the planner's group — plan 206 for the Test Group). New section **E — phase 5f** (read-only, every call expected to be refused before a write):
- planner `automation_TAX_decision` `{ tax_plan_id: classic, decision: "No" }` → 403 (body contains "VFO Services records")
- planner `automation_TAX_pricing` `{ tax_plan_id: classic, form_data: {} }` → 403
- planner `automation_TAX_extrameeting` `{ tax_plan_id: classic, outcome: "No" }` → 403
- planner `automation_TAX_sendagreement` `{ tax_plan_id: classic }` → 403
- planner `tax_save_task` `{ tax_plan_id: classic, task_id: 999999999, status: "Completed - No" }` → 403 (not on the list, plan not Direct)
- member `automation_TAX_decision` on `-DirectPlanId` `{ decision: "No" }` → 403 (ADMIN_ONLY — the member has no path to the decision any more)
- member `tax_direct_decision` and `tax_direct_amend_fee` on `-DirectPlanId` → whatever the dispatcher returns for an UNKNOWN action (find it in `router/dispatch.ts` / `index.ts` and assert that exact status; it must not be 200)
- planner `automation_TAX_amend_fee` `{ tax_plan_id: classic, stage: "tax4", keep: true }` → 200 or 400, NOT 403 (control: planners keep the amend step on classic — assert `HTTP -ne 403`; use the script's existing Check helper shape or add a `CheckNot`)
Update the `.SYNOPSIS`. Do not run the probe (it needs three passcodes); report the exact invocation for Jake.

### I. Docs (separate commit later — edit now, Fable commits)
- `docs/flows/tax-planning.md`: the planner-allowlist paragraph (~L1360, "Planners LOST `automation_TAX_decision`, … `_pricing`, `_extrameeting`, … `_sendagreement`") gains the 2026-09-21 Direct-only re-addition with the in-handler 403; the bell inventory row for `TAX_client_decision_needed` (~L1136, "The ONE PF bell that does not follow the member on Direct") and the `TAX_roi_skipped_decision_needed` row(s) say both go to the allocated planner on Direct; the Direct paragraph near L1086 gets one sentence on which steps are the Tax Team's on Direct (decision + Undecided follow-ups + both amend steps).
- `docs/plans/direct-tax-planning/README.md`: decision **29** in §2 (the two ownership changes, dated 09-21); §7 phase table row 5f → COMMITTED, action count 533 → 528, the five retired twins named; replace the "PHASE 5f — NOT STARTED" paragraph with a two-line "built as decision 29 — see unit2-phase5f-brief.md".
- Code comments in role-gates / dispatch / save-task / tax-direct-member as specified above (those go with the code commit).

### J. Verify and report
`deno check --no-lock` = 0; action count = **528** (6 + 522) — quote both numbers; `npm run build` exit 0; `grep -rn '__direct'` (edge function) = 0 hits; `grep -rn 'tax_direct_decision\|tax_direct_pricing\|tax_direct_extrameeting\|tax_direct_sendagreement\|tax_direct_amend_fee'` across BOTH repos' `src/`, `supabase/`, `scripts/` = 0 hits (docs history may keep them); `git status` in both worktrees. Report: every file touched with a one-line reason; the #448 select audit for F; the list of `stepOwner(` / `isPlannerEditable(` call sites; the admin-only controls audit from G.4; the import-cycle check from E.2; the unknown-action status from H. Then a click-through script for Jake with two logins: **Test Member 59524** (member portal, plan 207 = client 1818, Direct) and **Test TaxPlanner** (`test@taxplanner.com`, planner portal, plans 206 classic + 207 Direct, both in Test Group) — plan 207 already holds `tax_decision='Yes'`, agreement sent and retainer paid, so the decision test there is NOT possible on 207; write the script so item 1 (amend fee) is exercised on 207 and item 2 (decision) on plan 206 for the 403/lock (classic) and on a NEW Direct fixture Fable will name (a fresh Direct intake by 59524, walked to Tax 3), and say so.
