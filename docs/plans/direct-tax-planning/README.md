# DIRECT to Tax Planning — build plan, decisions and status

Branch `claude/vfo-session-setup-96fda1` (both repos). Written 2026-09-17 at the end of the first chat so the next chat has the whole picture. **This file is the plan of record for the project; the hub carries only live state.** Companion files in this folder: `unit1-brief.md`, `unit1b-brief.md` (the implementer briefs, verbatim), `direct-tax-planning-sheet-dump.txt` (Paul's spreadsheet, cell dump) and `licensing-structure-sheet-dump.txt` (Paul's licensing rules, cell dump — a future whole-system plan, NOT all in scope).

## 1. What the project is

Members may run a tax client themselves ("Direct"): the member is the PF, the tax planning team's role does not change. Alongside it, the whole tax intake moves into the portal (a 37-question form + $500 deposit replaces the disconnected Unbounce form), the deposit is split 50/50 with the tax planning team, ROI decks carry three logos, an "Additional Benefits" slide is added, clients get a portal login on payment, and a new standard agreement layout applies to all tax planning.

## 2. Jake's decisions — do NOT re-open

| # | Decision | Date |
|---|---|---|
| 1 | Storage: SAME tables (`clients`, `client_tax_plans`) + one nullable `client_tax_plans.tax_route` (NULL = classic, `'direct'`), copying the `roi_skip_mode` pattern. No second table for Direct clients. | 09-16 |
| 2 | Direct eligibility = 2 DISTINCT clients with `retainer_status='succeeded'` AND client's own green click at Client decision 1 (`post_review_client_decision` in Proceed/Confirmed/Auto-Locked) AND admin pick Continue/Undecided AND not refunded. Computed rule ONLY, no admin override. Shown clearly on admin and member side. Direct is an OPTION, never forced. Helper: `utils/tax-direct-eligibility.ts qualifyingTaxClients`. | 09-16/17 |
| 3 | The $500 deposit is paid by the MEMBER (route A) or by the CLIENT via emailed link (route B, added 09-17). Waived on BOTH routes once the member has 2 qualifying clients. Refund email addressed to whoever paid. | 09-16/17 |
| 4 | Green/Red Light step is the SINGLE trigger for both deposit money movements: Proceed → $250 transfer to the planning group's Connect account; Refund → full $500 refund via the linked PaymentIntent. Applies to ALL VFO Tax Planning plans with a deposit PI, including the 9 existing hand-pasted ones (option A). On Direct the MEMBER clicks it. | 09-17 |
| 5 | Manual `pi_` paste on the Deposit Paid step is KEPT beside the portal path. | 09-17 |
| 6 | Any member can start a tax client: the Tax Planning tab is visible to ALL members; the first intake creates the program-4 enrollment. | 09-17 |
| 7 | Standard clients use the same form; the member just has no control afterwards. Holistic clients: after the MAP 1 first payment the MEMBER gets an email with a link to the same form; answers attach to the existing client; no deposit; Holistic is never Direct. | 09-16 |
| 8 | Question 1 (advisor/accountant/client) is REMOVED from the UI; the server derives it (member type, or "Client" on route B). Q7–Q9 (introducer) are prefilled and hidden. Answers are READ-ONLY afterwards — nothing pre-fills any tax step. | 09-17 |
| 9 | VFOS allocates the tax planning team; the confirmation email always says "The tax planning team will be allocated in due course." Allocation stays admins + Team Members (Tax Planners read-only, members never). | 09-17 |
| 10 | On Direct the member does EVERYTHING our team normally does; tax-team steps render DISABLED with the planner-style red circle-slash + hover warning; VFOS keeps CEO countersign + split edits. Direct pricing preset 45/45/10 (member/group/VFOS), thirds hidden. | 09-16 |
| 11 | $300 VFO Portal licence at Client decision 1 on EVERY tax client: taken from the retainer pool FIRST, split after; stays in the VFO Services balance short-term (no transfer); disbursement line on the revenue-share invoice; client gets 12 months free portal membership (login + visible expiry date shown when they log in, nothing billed) and the member may run CIQ on that client. Licence must be its OWN record (`client_portal_licenses`) — whichever of Holistic/Tax comes first wins, a later purchase EXTENDS pro-rata by days, a tax refund revokes it. | 09-16/17 |
| 12 | Client portal login + free-membership paragraph go in the INVOICE/RECEIPT email for card, ACH and check, all tax. Training video URL not yet available. | 09-16 |
| 13 | Logos on SLIDES ONLY: group bottom-left, member bottom-middle (member uploads on profile, toggle default OFF, blank when off), VFO bottom-right; title slide logos at the TOP. Additional Benefits slide on ALL decks. Book-ends (branded title + closing slide) for the hand-made detailed deck. | 09-16/17 |
| 14 | ONE contract layout for ALL tax planning (no separate Direct row): opening paragraph gains the collaborating-team sentence (group, member unless opted out / personal name fallback, VFO Services); Steps 1–3 name the member as introducer/presenter; Step 4 says "Meeting with Advanced Tax Planner"; new "Additional Benefits" section after Double Guarantee (slide wording, no check marks, no "$2,600" line, + the Q4 new-engagement sentence); title "Tax Planning Engagement Agreement (Direct)" approved earlier is MOOT since one layout serves all. All four existing agreement rows get re-seeded. | 09-17 |
| 15 | Reminders (planner review 2-day chase, additional-info chase) are IN scope; on Direct they name the member as PF. | 09-17 |
| 16 | Every outbound email ends with `VFO Services - Proactive Coordinator Team` (VFO_SIGNATURE), enforced in the delivery layer. | 09-17 |
| 17 | Every payment issues the house invoice + receipt PDF pair (numbered, Drive, attached) — never a Stripe receipt link. | 09-17 |
| 18 | Deposit memos: incoming "VFO Tax Planning Deposit - Client: X - Member: (n) Y"; transfer **"Tax Planning Revenue Share - Client: (ref) X - Tax Planner: P — Group - Deposit"** — the planner leg's shape with `Deposit` where it says Retainer / Implementation, so a plan's three transfers sort together (the 09-17 "Tax Planning Deposit Team Share ($250 of $500) - …" wording is superseded); also stamped on the group's destination payment. | 09-17, memo 09-18 |
| 19 | The deposit invoice / receipt NUMBERS are NOT displayed on the Deposit Paid step. They exist on `tax_intake_requests` and in Drive only; the emails carry the PDFs. | 09-18 |
| 20 | The Green/Red Light step is listed on EVERY surface, the member view included (it used to hide from the read-only track until a refund landed). The member view shows the outcome only — the Proceed / Refund buttons are gated on `!readOnly`. | 09-18 |
| 21 | The "Refunded $<amount>" chip on the Green/Red step is RED (`#e74c3c`; it was green). | 09-18 |
| 22 | Deposit refund email subject is `VFO Services - Tax Planning Deposit Refunded - [Client Name]` on BOTH 181 and 279 — the client's name at the END after a dash, the house " - Name" shape, never in parentheses. Applied live by `execute_sql` and committed as `20260918140000`. | 09-18 |
| 23 | The member view of a tax plan shows the allocated **Team Member** chip beside the Tax Planner chip — `tax_load_plans` now returns `tax_planner_name` + `team_member_name` on every plan. | 09-18 |
| 24 | A WAIVED intake closes the Deposit Paid step as `N/A — No Deposit` (finalize writes the progress row; the step renders it as a chip with no `pi_` box; the Client Overview done-test accepts it). The Green/Red Light step then offers **Proceed only** (no Refund — nothing to refund) and its sub-label reads "No deposit was taken for this client". Found by the wrap-up doc audit, not by the test: before this a waived plan could never leave Set Up. Shipped as v864. | 09-18 |
| 25 | The 33 live plans that had ALREADY Proceeded before the $250 leg existed (deposits split by hand months earlier) are stamped `deposit_team_share_status = 'N/A — Legacy'`, a new TERMINAL value (v865), so a date edit or a planner change can never forward $250 retroactively. The 9 undecided plans stay live per decision 4. Found by the Fable post-ship review: the Proceed hook fires on any re-save of the step, and the allocation release fires on any planner change while the leg is empty. Migration `20260918170000` (data only). | 09-18 |

Ride-along, not a decision: the three member-reachable tax loaders (`tax_load_plans`, `tax_load_progress`, `tax_load_specialists`) gained a **member ownership guard** — `denyIfNotOwnClient` / new `denyIfNotOwnPlan` in `utils/client-ownership.ts` (plan → client → `member_number` vs the session, never the body). They carried only the planner-group guard, which passes every non-planner caller through (the #455 class). Admins and planners are unaffected.

## 3. The four shipping units

| Unit | Scope | Status |
|---|---|---|
| **1 Intake + deposit** | 37-question form (routes A member / B client link / Holistic email), $500 Checkout, waiver, auto client+enrollment+plan, deposit invoice+receipt pair, confirmation emails, $250 team leg, linked refund, `tax_route` column, footer guard, security ride-along (5 tax writes closed to members), test-member fill button, member ownership guard on the three member-reachable tax loaders | **SHIPPED** — `vfo-admin-api` **v863** live (v860 → v863, 2026-09-17/18), 4 of 4 migrations + the 09-18 subject migration applied, test matrix in §4 complete on 2026-09-18, fixtures wiped; the shipping tag is derived from git after merge |
| **2 Direct execution** | Route choice Direct on the form when eligible, eligibility shown both sides, member runs every VFO-team step via NEW narrow handlers (`denyIfNotOwnClient` + route check), tax-team steps disabled, 45/45/10 preset, member as PF (NOTE: `clients.assigned_pf` is a NAME resolved by `utils/pf-emails.ts` — a member name resolves to "" silently; needs a member fallback or a flag column) | not started |
| **3 Branding + contract** | `tax_planning_groups.logo_image` + admin upload on the Tax Planning Partners card; `members.logo_image` + `logo_enabled` + `contract_name_enabled` via a NEW narrow member self-save action (none exists today); deck v9 master (logos as `<p:pic>` on all slides, top row on title, Additional Benefits slide baked in; only the member PNG bytes swapped at runtime, transparent PNG when off; re-derive SHIFT_EXPECTED constants, #480); book-ends generator; re-seed the 4 agreement rows with the new layout (sandbox BoldSign send per row to re-verify field_map pages, #439) | not started |
| **4 Licence + client portal + reminders** | `client_portal_licenses` table; $300 disbursement before the split at Client decision 1 (every split surface must show it, #465); invoice line; auto client login + portal email paragraph + expiry shown in the client portal; revoke on refund; CIQ on that client; the two reminders | not started |

Sequencing note: clients created between unit 1 and unit 4 get no licence/$300 — accepted.

## 4. Unit 1 — shipped state (2026-09-18)

**Live:** `vfo-admin-api` **v864** (v860 = unit 1 + memos; v861 = unit 1b client route; v862 = deposit documents + intake-load; v863 = the 09-18 UI/copy decisions, transfer memo, loader guards; v864 = the waived-plan Deposit Paid row, decision 24). Frontend from this worktree, last WIP commit `d717f01`.

**Migrations (files in `supabase/migrations/`):**
| File | Applied live? |
|---|---|
| `20260917120000_tax_intake.sql` — `tax_intake_requests` (RLS deny-all, anon `*/0`, advisor GREEN strong check), `client_tax_plans.tax_route` + `deposit_team_share_*`, `clients.tax_intake_requested_at`, rule `FAILURE_tax_deposit_team_share` | YES (ledger `20260917204033 tax_intake`) |
| `20260917120100_tax_intake_email_templates.sql` — templates 275 `TAX_new_case_confirmation`, 276 `TAX_intake_request\|holistic`, 181 re-addressed to MEMBER, six AI-PC footer blocks stripped (0 left) | YES (`20260917204245`) |
| `20260917200000_tax_intake_client_route.sql` — `payer`, `intake_token`, `link_sent_at`, `link_opened_at`; templates 277 `TAX_intake_link`, 278 `TAX_new_case_confirmation\|client`, 279 `TAX_deposit_refund\|client` | YES (`20260917211229`) |
| `20260917210000_tax_intake_deposit_docs.sql` — five `deposit_*` doc columns + strips `[RECEIPT_BUTTON]` from 275/278 | YES (`20260918132530`, after v862) |
| `20260918140000_tax_deposit_refund_subject.sql` — subject of 181 + 279 → `… Refunded - [Client Name]` (decision 22) | YES — applied by `execute_sql` on 09-18, so it has **no ledger row**; the file is guarded on the old subject and re-runs as a no-op |

**Actions:** 507 → **514** (`tax_intake_eligibility`, `tax_intake_submit`, `tax_intake_holistic_submit` MEMBER_SCOPED; `tax_intake_load` in-handler per role + on TAX_PLANNER_ALLOWED; `tax_intake_send_link` MEMBER_SCOPED; `tax_intake_link_load` / `tax_intake_link_submit` PUBLIC, token = whole credential #310). Route pages 34 → **35** (`/tax-intake`). Security ride-along: `tax_start_plan`, `tax_save_task`, `tax_save_deposit_pi`, `tax_add_specialist`, `tax_remove_specialist` added to ADMIN_ONLY_ACTIONS (planners unaffected); five dead `msm_*` tax names removed (#256).

**Test matrix — results 2026-09-18 (Jake, dev server on the react worktree, Test Member 59524 sandbox-forced, vs v862 / v863):**
| Step | Result |
|---|---|
| Smoke gate | PASSED 5/5 (vs v860, v861, and again vs v862/v863) |
| `scripts/probe-tax-intake-refusals.ps1` — member token on the 5 closed tax writes → 403; forged `client_id` / `tax_plan_id` on the member loaders → 403; public token misuse | PASSED 19/19 |
| Route A on v862: chooser, q1 gone (stored Advisor for the client), form, $500 Checkout paid, success card, client listed; admin Deposit Paid "Paid via portal" + form card; confirmation 275 with the docs pair attached (`INV-59524-008-0062` / `REC-59524-008-0001`) | PASSED |
| Green/Red Proceed → $250 transfer `tr_1UH2F7Rv8yMNbvOJTCy763TT`, `Yes` chip; re-click no-op | PASSED |
| Refund → member-addressed 181 draft, red Refunded chip, `stopped` | PASSED |
| Route B end to end: send link → 277 draft → public page → pay → thank-you (ordering fix: the paid return wins over the completed status) → admin card "Completed by the client" → 278 confirmation to the client → 279 refund to the client; 409 on a second submit; re-send reuses the token | PASSED |
| Waived on A and B (two fixture plans stamped qualifying by SQL, then reverted) → no Checkout, `waived` | PASSED on v863 — but the plan could NOT be advanced: Deposit Paid was left open and every downstream gate keyed on it (found by the doc audit; decision 24, fixed in v864) |
| Waived plan ADVANCED end to end on v864: Deposit Paid `N/A — No Deposit` chip, Request Tax Returns unlocked, allocation, Green/Red offers Proceed only, Proceed → team-share `N/A — No Deposit`, nothing moved (fixture plans 201/202 inserted qualifying by SQL, deleted after) | PASSED |
| Holistic entry by URL `?intake_client=62` → thank-you; second submit → 409 | PASSED (the URL only — the MAP 1 first-payment trigger itself is code-only) |
| Footer guard: one standard footer, once, on the old refund template (181) | PASSED |
| Test-member "Fill with test values" button, member form AND public page | PASSED |
| Holistic `TAX_intake_request\|holistic` fired by a real MAP 1 first payment | code-only (v860) |
| `Awaiting Planner Allocation` arm (Proceed with no planner — the allocation step sits above Green/Red in the UI) + its release in `allocate-planner.ts` | code-only (v860) |
| `FAILURE_tax_deposit_team_share` bell (transfer refused) / `FAILURE_tax_deposit_docs` bell (number not reserved) | code-only (v860 / v862) |
| `checkout.session.expired` → `expired` row | code-only (v860) |
| Every REAL (non-sandbox) send of 275–279 / 181 | code-only — all five new rows are `send_mode=false`, no live member has submitted |
| The 9 existing hand-pasted deposit plans: first real Proceed forwards $250 | production consequence, unwatched |

**Fixtures — WIPED 2026-09-18:** clients 59524-007..011, plans 195–200, `tax_intake_requests` 1–6 (table is empty), 33 bells, the enrollment rows. **Still owed by hand (Jake):** the Gmail drafts on the sandbox inbox, the two Drive client folders, the sandbox Stripe session / PI / transfer / refund objects. The `document_numbers` reservations were deliberately left. Client 62 holds 0 tax plans.

**Docs ripple — done 2026-09-18** across `tables/tax.md` (`tax_intake_requests` + the four `client_tax_plans` columns), `tables/clients.md`, `tables/documents.md` (275–279, 181, the strips, `document_numbers`), `tables/notifications.md`, `integrations/stripe.md` / `gmail.md` / `google-drive.md`, `glossary.md`, `README.md`, this file; the hub / CHANGELOG / GOTCHAS / flows / architecture entries are the other writers' slices of the same ripple. `scripts/emit-route-pages.mjs` changed for the new route.

## 5. Known gaps / decisions still owed
- Vault copy of the deposit PDFs: none (the retainer pair does not do one either).
- `FAILURE_tax_deposit_docs` bell has no `notification_rules` row (falls back to Jake; cannot be re-routed or disabled from the Editor until seeded). It fires only on the number-not-reserved case; a failed PDF render skips the pair with a log line and no bell.
- The `send_mode` for `TAX_intake_link` is Draft: a member's "Send link" only creates a Gmail draft until Jake flips it. Same for 275 / 276 / 278 / 279 — none of the six intake-side emails has had a real send.
- `tax_intake_requests.stripe_account` is stamped (#485) but **nothing reads it**: the refund path is the pre-existing `automation_TAX_depositrefund`, which reads the primary key inline. Correct while `TAX` stays `'vfos'`; a `TAX` → `ert` move must teach that handler the stamp first.
- Code-only after the 09-18 matrix: the Holistic MAP 1 trigger, the `Awaiting Planner Allocation` arm, both failure bells, the `checkout.session.expired` branch, every real send. The first real Proceed on one of the 9 hand-pasted deposit plans forwards $250 with nobody watching.
- The deposit refund is full-amount only — `refund_override_amount` (v848) is the Client decision 1 refund's, not the deposit's.
- Unit 2 must solve `assigned_pf` = member (see §3), and is the first writer of `tax_route`.
- Petrus hotfix leftovers (separate branch, shipped v859): `TAX_amended_invoice_not_sent` bell never fired live; its rule description is stale; 13 migrated plans carry planner leg `N/A — No Share Due` (Jake's call).

## 6. Key code map (unit 1)
Backend `supabase/functions/vfo-admin-api/`: `actions/tax/intake-*.ts` (7 handlers), `utils/tax-intake-questions.ts` (the 37 questions + validation, MIRRORED in `src/components/member/taxIntakeQuestions.js` — keep byte-equivalent), `utils/tax-direct-eligibility.ts`, `utils/tax-intake-finalize.ts` (client + enrollment + plan + Deposit Paid + docs + confirmation; latch `created_client_at`), `utils/tax-deposit-docs.ts`, `utils/tax-deposit-team-share.ts` ($250 leg, own columns, key `deposit-team-tax-<plan>`, probe before key), `utils/tax-intake-request-email.ts` (Holistic email, stamp `clients.tax_intake_requested_at`), `utils/tax-intake-link-email.ts`, `utils/email-signature.ts` (footer guard in `draftGmail` / `gmailDraftFetch` / `deliverRaw`), `constants/member-types.ts`; hooks in `actions/tax/save-task.ts` (Proceed → $250), `actions/tax/allocate-planner.ts` (release), `actions/tax/revshare-sweep.ts` (third pass), `actions/tax/deposit-refund.ts` (payer-aware), `actions/pipeline/contract-invoice-receipt.ts` (Holistic hook), `router/webhooks.ts` (two branches: `payment_kind=tax_intake_deposit` completed/expired), `utils/client-ownership.ts` (`denyIfNotOwnClient` + NEW `denyIfNotOwnPlan` — the member ownership guard on `actions/tax/load-plans.ts` / `load-progress.ts` / `load-specialists.ts`, 2026-09-18), `actions/tax/load-plans.ts` (now returns `tax_planner_name` + `team_member_name` per plan, resolved from `tax_planners` — the member view's chips). Repo-root `scripts/probe-tax-intake-refusals.ps1` (19 refusal probes: the 5 closed tax writes, forged ids on the member loaders, public-token misuse — run it against any deploy that touches tax gates). Frontend: `src/components/member/TaxIntakeForm.jsx` (chooser, form, link mini-form, publicMode, test fill), `src/pages/TaxIntakePage.jsx` (`/tax-intake`; paid return wins over the completed status), `MemberMSMTracking.jsx` / `MemberPortal.jsx` (tab always visible, return handling), `TaxPrioritiesTab.jsx` (form card, "Paid via portal" / "Completed by the client" chips, Green/Red on every surface, red Refunded chip, team member chip in the member view — doc numbers deliberately NOT shown).
