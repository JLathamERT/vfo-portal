# UNIT 2 — PHASE 5 BRIEF — the member's Direct tax plan screen (+ contacts and notes on a Direct client)

You are the implementer (Opus). Fable/Jake review. Read `unit2-phase0-brief.md` "Absolute rules" first — they apply verbatim. Edge worktree `C:\vfo-edge-functions\.claude\worktrees\vfo-session-setup-7559c8`, react worktree `C:\vfo-react\.claude\worktrees\vfo-session-setup-7559c8`. Phases 0–4 are committed on this branch (edge HEAD `d41a606`, react HEAD `4df9733`); build on the current trees.

Read first: `docs/flows/tax-planning.md` (Tax 1 order, Step 0b, the planner-portal "visible-but-locked" section), `docs/plans/direct-tax-planning/README.md` decisions 4, 10, 15, 20; `constants/role-gates.ts` `TAX_DIRECT_MEMBER_ACTIONS`; `utils/tax-direct-member.ts`; `actions/tax/direct-save-task.ts` and `DIRECT_MEMBER_EDITABLE` in `actions/tax/save-task.ts`; `docs/GOTCHAS.md` #262 #263 #339 #459 #487 #490 #495.

## What exists
- Backend (phase 4): 15 `tax_direct_*` actions (`TAX_DIRECT_MEMBER_ACTIONS`), each a wrapper that 403s unless caller is a member, the plan is the caller's own, and `tax_route='direct'`; `tax_direct_save_task` is a narrow twin allowing only the steps in `DIRECT_MEMBER_EDITABLE` (sentinels `tax_refund` Proceed-only, `enter_details`, `tax_amend_fee`, `tax_amend_fee_tax5`; names "Client risk profile complete", "ROI Presentation"); refuses `Stopped`/`N/A` as input and as existing value. `tax_direct_sendagreement` exists and the three decision chains route through it when the request came via a wrapper.
- Frontend today: `TaxPrioritiesTab.jsx` `TaxPlanTrackView` takes `readOnly` (member) and `plannerMode` (planner). `ClientDetail.jsx` mounts it with `readOnly={isMember}`. Members see everything read-only. `plannerMode` locks non-whitelisted steps with a silent inert wrapper (cursor not-allowed + pointerEvents none).
- `LockedIcon` (red circle-slash SVG) + `lockedHintStyle` already exist in the file for prerequisite locks.

## Decisions (Jake, final)
- A Direct member runs every VFO-team step: Request Tax Returns / already have, Green/Red Light (Proceed + Refund), Client risk profile, ROI meeting booked / skip ROI, Generate + download presentation, Send presentation link, Client tax planning decision (Yes / Undecided / No) + Undecided follow-ups (pricing / extra meeting), Detailed tax plan meeting confirmation (HLM), both Amend fee steps, ROI Presentation. Their Proceed moves the $250 team share exactly as an admin's (confirmed 2026-09-18).
- Tax-planning-TEAM steps stay locked for the member: Allocate Team Member / Tax Planner, Additional information required, Tax planner review complete, Detailed tax plan presentation, Client decision 1, Allocate to VFO Specialist + every per-specialist row (Tax 5a/5b/6), Implementation decision, Client decision 2. VFOS-only: CEO countersign, the pricing split card, Live/Stopped toggle, +Start Tax Plan, the $250 team-share chip, StepEmailsChips, stall-ack controls — all stay hidden/inert as they are for `readOnly` today.
- Locked steps render **visible with the red circle-slash `LockedIcon` and a hover/hint "Handled by the Tax Planning Team"** (decision 10) — NOT the planner's silent wrapper.
- Direct members may add/edit/delete **Additional Contacts** on their own Direct client and may add **Notes** on it (their notes are `visibility='shared'` so admins see them; they read only shared notes, as today).

## Build

### A. Frontend — `directMode`
1. `ClientDetail.jsx`: compute `isDirect = isMember && !!client?.pf_member_number && String(client.pf_member_number) === String(session member number)` (find how the member session is read there; `getSession()` from lib). Pass `directMode={isDirect}` to `TaxPrioritiesTab` alongside `readOnly={isMember}`.
2. `TaxPrioritiesTab.jsx` → `TaxPlanTrackView`: new prop `directMode`. Define `const memberRuns = directMode && !readOnlyForStep` … concretely: introduce `const editable = !readOnly || directMode` for the VFO-team steps listed above and keep `readOnly` semantics for everything else. Implement it as a per-task predicate `isDirectEditable(task)` mirroring `DIRECT_MEMBER_EDITABLE` (sentinel first, then exact name) PLUS the non-save-task steps (`tax_returns_request`, `tax_3_decision`, `tax_generate_presentation`, `tax_presentation_link`, `enter_details`, `tax_hlm_confirm`, `tax_amend_fee`, `tax_amend_fee_tax5`, `tax_refund`). Put the list in ONE const at the top with a comment that it is a cross-repo contract with `actions/tax/save-task.ts DIRECT_MEMBER_EDITABLE` (#339 family).
3. Action remap: in `directMode`, every call the editable steps make goes to the `tax_direct_*` twin. Do this with ONE helper `const act = (name) => directMode ? DIRECT_ACTION_MAP[name] || name : name` and route each `callApi(...)` for those steps through it. Map (admin → direct): `automation_TAX_request_returns→tax_direct_request_returns`, `automation_TAX_returns_already_have→tax_direct_returns_already_have`, `automation_TAX_depositrefund→tax_direct_deposit_refund`, `automation_TAX_readyfortax3→tax_direct_readyfortax3`, `automation_TAX_skiproimeeting→tax_direct_skiproimeeting`, `tax_generate_presentation→tax_direct_generate_presentation`, `tax_presentation_downloaded→tax_direct_presentation_downloaded`, `automation_TAX_presentation_schedule→tax_direct_presentation_schedule`, `automation_TAX_decision→tax_direct_decision`, `automation_TAX_pricing→tax_direct_pricing`, `automation_TAX_extrameeting→tax_direct_extrameeting`, `automation_TAX_highlevelmeeting_confirm→tax_direct_highlevelmeeting_confirm`, `automation_TAX_amend_fee→tax_direct_amend_fee`, `tax_save_task→tax_direct_save_task`. The `tax_save_task` remap applies ONLY on the Direct-editable steps; any other `tax_save_task` call in directMode must not fire (the step is locked anyway).
4. Locked rendering: in `renderTask`, when `directMode` and the task is NOT direct-editable, wrap the node like the plannerMode wrapper (outer `cursor:'not-allowed'`, inner `pointerEvents:'none'`, #263) AND prepend a row header line with `<LockedIcon/>` + hint text "Handled by the Tax Planning Team" in `lockedHintStyle`. Reuse the exact markup the prerequisite gate uses for its hint so the two look identical. Keep the `readOnly` catch-all ordering in mind (#459): the directMode branch must run BEFORE the `if (readOnly) return …` catch-alls on the editable steps — audit each editable step's renderer and report where the catch-all sits.
5. Prerequisite gate (`stepGate`): today members are exempt (`if (!readOnly && !alreadyDone)`). In directMode apply the gate to the editable steps exactly as for an admin (`!readOnly || directMode`).
6. Green/Red on Direct: Proceed + Refund buttons enabled (they are gated on `!readOnly` today → make it `!readOnly || directMode`); the `$250 team share` chip stays admin-only. Refund's confirm text unchanged.
7. Hide in directMode exactly what readOnly hides: pricing split card, Live/Stopped toggle, +Start Tax Plan, StepEmailsChips, stall-ack, specialist Strategy input, the team-share chip.
8. `src/lib/api.js` `timeoutFor()`: give the `tax_direct_` prefix the same 60 s tier as `automation_`, and mirror the explicit entries `tax_generate_presentation` / `tax_save_task` carry onto `tax_direct_generate_presentation` / `tax_direct_save_task`. Do NOT add any retry (writes never retry).
9. Decision form `notes`: the admin Tax 3 decision step saves `notes: JSON.stringify(formData)` with the status through `tax_save_task`. The twin rebuilds the body without `notes`. Widen `actions/tax/direct-save-task.ts` to pass `notes` through ONLY for the `enter_details` sentinel (string, ≤ 20 000 chars, else 400) — report the hunk.

### B. Contacts + notes for a Direct member (backend + frontend)
1. Backend: `actions/msm/add-client-contact.ts`, `update-client-contact.ts`, `delete-client-contact.ts` — read each; they are `MEMBER_SCOPED`? / `ADMIN_ONLY`? (grep role-gates and report). Add, for a `member` caller: `denyIfNotOwnClient` on the contact's `client_id` (resolve the contact's client for update/delete from the row, never the body) AND a check that `clients.pf_member_number === auth.callerMemberNumber` (Direct only) → else 403. If they are ADMIN_ONLY, do NOT widen the gate: create `tax_direct_contact_add` / `_update` / `_delete` twins through `directMemberHandler` with `planKey` replaced by a client-keyed variant — add `directMemberClientHandler(inner)` in `utils/tax-direct-member.ts` that resolves the client's Direct-ness by `clients.pf_member_number === auth.callerMemberNumber` instead of a plan's `tax_route` — and add the three to `TAX_DIRECT_MEMBER_ACTIONS`. Report which path you took.
2. Notes: `add_client_note` is ADMIN_ONLY. Add `tax_direct_note_add` via `directMemberClientHandler` wrapping a thin function that forces `visibility='shared'` and `created_by` = the member's display name, then calls the real add handler's core. Members keep reading via `load_client_notes_shared`. No update/delete for members this phase.
3. Frontend: `ClientDetail.jsx` Profile tab in directMode: render the `ClientAdditionalContacts` card in EDIT mode (it renders read-only for members today — find the prop) and route its calls through the direct names; the notes card gets an "+ Add Note" for a Direct member that calls `tax_direct_note_add` (shared only; no visibility picker).
4. Probe: add to `scripts/probe-tax-intake-refusals.ps1` section D: member token on `tax_direct_note_add` with a CLASSIC own client (`$OwnClientId`=62) → 403; admin token on it → 403.

### C. Verify
`deno check --no-lock` → 0; action count → report (expect 530 + the contact/note twins you added); `npm run build` exit 0; list every `callApi(` in `TaxPrioritiesTab.jsx` that still names an `automation_TAX_*` or `tax_save_task` action without going through `act()` and state why each is fine (admin-only surface, or hidden in directMode).

## Report back to Fable with
files changed (both repos), the `DIRECT_EDITABLE_TASKS` const + the `DIRECT_ACTION_MAP`, the locked-row markup, the #459 catch-all audit, the timeoutFor hunk, the direct-save-task `notes` hunk, the contacts/notes path taken with the new action names and gates, the probe lines, the three gate results verbatim, and a numbered click-through for Jake on Test Client 1818 (59524-008, plan 207, already `tax_route='direct'`): every editable step in order, one locked step, contacts add/edit/delete, a shared note, and the admin view of the same plan afterwards.
