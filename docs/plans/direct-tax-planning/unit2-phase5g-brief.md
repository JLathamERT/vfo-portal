# UNIT 2 — PHASE 5g BRIEF — "Client risk profile complete" is a prerequisite of "ROI Meeting booked - Send Confirmation Email" (every plan)

You are the implementer (Opus). Fable planned, Fable reviews. Read `unit2-phase0-brief.md` "Absolute rules". Edge worktree `C:\vfo-edge-functions\.claude\worktrees\vfo-session-setup-7559c8`, react worktree `C:\vfo-react\.claude\worktrees\vfo-session-setup-7559c8`. BOTH worktrees carry UNCOMMITTED phase-5f changes — do not revert, stash or commit anything; add on top. Do not deploy, migrate or write SQL. Dev server on http://localhost:5174 is running; leave it.

Jake, 2026-09-21: on EVERY tax plan (classic and Direct, program 1 and 4), the Tax 1 step "Client risk profile complete" must be answered before the Tax 2 step "ROI Meeting booked - Send Confirmation Email" (sentinel `tax_3_decision`) can be answered. Lock + hint on the FE row (#403: a lock is choreography), a 400 in the handler (the boundary).

Read first: `src/components/admin/tax/TaxPrioritiesTab.jsx` — the chain vars ~L2800–2960 (`depositOk`, `roiBooked`, `diagnosticChain`, `reviewStop`), `stepGate` (~L2965+, the `tax_3_decision` branch), the generate-presentation card's `riskTask` / `riskSet` (~L3465), `renderTask`'s use of `stepGate` (~L3233–3240, note `isRoiBookingStep`); edge `actions/tax/ready-for-tax3.ts` L40–80 (the guards), `actions/tax/generate-presentation.ts` L140 + ~L620–632 (`RISK_TASK_NAME` and how it reads the progress row), `actions/tax/save-task.ts` ~L470–485 (the review Proceed bell text "Book the ROI meeting for the client."), `utils/tax-plan-steps.ts` L34 + ~L528–532 (`RISK_PROFILE_TASK` in the lane head), `docs/flows/tax-planning.md` ~L105–125 (the step-gate table).

## Build
1. **FE chain var.** Beside `roiBooked` add `riskProfileDone`: the plan-level progress row (no specialist) of the task named `'Client risk profile complete'` has a status containing `'Risk'` — the same test the generate card's `riskSet` makes. Replace that card's local `riskTask`/`riskSet` with the hoisted var (one source).
2. **FE gate.** `stepGate`'s `tax_3_decision` branch: locked when `!(diagnosticChain || reviewStop) || !riskProfileDone`. Hint: the existing `'Complete "Tax planner review complete" first'` when the chain is not there, else `'Set the "Client risk profile complete" step first'`. Update the branch comment (the risk profile feeds the ROI deck, so it is settled before the meeting is booked; the admin Skip button shares the row and waits too — deliberate).
3. **Backend 400.** `ready-for-tax3.ts`, directly after the allocation guard (~L76–78): for `decision !== "declined"`, read the plan's progress row for `RISK_TASK_NAME` exactly as `generate-presentation.ts` does (copy its query; if its test is "any status" vs "contains Risk", match `generate-presentation.ts`, and report which). Missing/empty → `json({ error: 'Complete the "Client risk profile complete" step before booking the ROI meeting' }, 400)`. A decline is NOT gated (same asymmetry as the allocation guard).
4. **Bell wording.** `save-task.ts` ~L479: if "Book the ROI meeting for the client." is the bell's MESSAGE, change it to `Set the client risk profile, then book the ROI meeting for the client.`; if it is the TITLE, leave it byte-identical (the clear in `ready-for-tax3.ts` / `skip-roi-meeting.ts` matches `Book the ROI meeting for%`) and report that instead. Check the Direct variant of the same bell (the member-addressed copy) and treat it the same way.
5. **Step machine.** `utils/tax-plan-steps.ts` lane head already lists `RISK_PROFILE_TASK` before `tax_3_decision`. Read `laneHead` and confirm it returns the FIRST not-done step in order (so the overview's next action already reads "Client risk profile complete" first). No code change unless that is false — report.
6. **Docs.** `docs/flows/tax-planning.md` step-gate section: add the row/sentence for the new prerequisite (and the 400). One line in `docs/plans/direct-tax-planning/README.md` §7 ride-alongs.

## Verify + report
`deno check --no-lock` 0; `npm run build` exit 0; action count unchanged (report it); the exact hunks for 1–4; the laneHead answer; git status of both worktrees (the 5f files must still be modified, plus yours). Click test for Jake on plan 211 (Direct, member portal, risk profile unset): the booking row is locked with the risk hint; set the risk profile; the row unlocks; book. Then on a CLASSIC plan (206, admin) the same lock/unlock.
