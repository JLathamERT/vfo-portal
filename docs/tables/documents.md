# Documents & templates tables

Three small tables, each underpinning multiple pieces of automation across pipelines.

## `agreement_templates`

BoldSign template configuration per (service_level × payment_plan × pipeline). Used by `automation_CONTRACT_sendagreement` AND the parallel send-agreement handlers for tax, advisor, and accountant.

| Column | Type | Notes |
|---|---|---|
| `id` | integer | pk |
| `service_level` | text | not null. e.g., `Lite` / `Core` / `Max` for MAP1; `Advisor` for advisor onboarding; `Accountant` for accountant onboarding. |
| `payment_plan` | text | not null. e.g., `OneTime` / `Quarterly` for MAP1; `Single` for advisor; `'No accountant partnership'` / `'Accountant Partnership'` for accountant (acts as the partnership-branch discriminator). |
| `title` | text | not null. Template display name. |
| `html_body` | text | not null. HTML template with placeholder tokens (substituted before PDF render). `[ACCOUNTANT_NAME]`, `[ACCOUNTANT_EMAIL]` for accountant agreements; analogous for the other pipelines. The `[EFFECTIVE_DATE]` token was removed (2026-06-15) from the advisor (id 9) and accountant (ids 10/11) `html_body` rows — and the rendered bucket PDFs regenerated — so those agreements no longer carry an effective-date line. **2026-07-30 (advisor id 9):** the two literal `$4,000` implementation fees — one in the VFO Fast Track section, one in the PFT section — were replaced by `[IMPLEMENTATION_FEE_VFO_FT]` and `[IMPLEMENTATION_FEE_PFT]`, filled from `advisor_onboarding.implementation_value_vfo_ft` / `_pft`. **The suffixes are load-bearing:** `actions/tax/send-agreement.ts` already substitutes an unrelated token named `[IMPLEMENTATION_FEE]`, and substitution is a flat `String.replace` over `html_body` with no pipeline namespacing — grep every `send-agreement.ts` before naming a new token. The substitution emits the NUMBER only; the `$` stays in the HTML. Gotcha #307. |
| `boldsign_template_id` | text | nullable. Vestigial in the current send flow — the handler renders `html_body` to PDF via html2pdf.app and uploads to BoldSign directly. Currently NULL on accountant rows; the handler does not require it. |
| `field_map` | jsonb | not null. Maps placeholder keys → BoldSign field coordinates `{p, x, y, w, h}` per field. Accountant field keys: `vfo_ft_checkbox`, `corporate_checkbox`, `accountant_signature`, `accountant_print_name`, `accountant_date`, `accountant_address`, `ert_signature`, `ert_date`. (No `pft_checkbox` — dropped from accountant flow.) |
| `active` | boolean | default `true` |
| `pipeline` | text | not null. Pipeline discriminator added 2026-05-22 when Tax Planning was branched. Values: `'MAP 1'` (implicit), `'TAX'`, `'ADVISOR_ONBOARDING'`, `'ACCOUNTANT_ONBOARDING'` (two rows per pipeline for accountant — one per partnership branch). |
| `payer_type` | text | default `'client'`. Values `'client'` \| `'member'`. Distinguishes the standard client agreement from the member-paid-on-behalf variant (see `pipeline_map1.member_paying_on_behalf`). |
| `created_at` | timestamptz | default `now()` |

**Constraints:** the UNIQUE constraint was widened from `(pipeline, service_level, payment_plan)` to `(pipeline, service_level, payment_plan, payer_type)`.

**Rows:** 6 MAP 1 rows (ids 14–19) are `payer_type='member'` (Lite/Core/Max × Quarterly/1-Time, with BoldSign template ids + field_maps); existing rows were backfilled to `payer_type='client'`. The **TAX** pipeline also now has a `payer_type='member'` row (**id 20** — service_level `Tax Planning`, payment_plan `Single`, BoldSign template `3e575f15-...`) alongside the client row (id 8). The member HTML carries a `<!--MC-->...<!--/MC-->` member-contribution block that `actions/tax/send-agreement.ts` strips at render so the PDF matches the member field_map coordinates; the handler filters `.eq("payer_type", memberPays ? "member" : "client")` (required now that two TAX rows exist). **Row 22 (added 2026-07-30) is an INACTIVE BACKUP** of the advisor agreement as it read before the implementation-fee tokens: `pipeline='ADVISOR_ONBOARDING'`, `payment_plan='Backup 2026-07-30'` (the `(pipeline, service_level, payment_plan, payer_type)` unique constraint is why the `payment_plan` had to differ), title suffixed `(backup 2026-07-30 pre-implementation-fee)`, **`active=false`**. It **must stay inactive** — `actions/advisor/send-agreement.ts` selects `.eq("active", true).order("id", {ascending:false}).limit(1)`, so activating row 22 would beat the live id 9 and silently ship the old hardcoded-$4,000 agreement. Restore by copying its `html_body` back onto id 9, never by flipping `active`.

**Touched by:** `automation_CONTRACT_sendagreement`, `automation_TAX_sendagreement`, `automation_ADVISOR_sendagreement`, `automation_ACCOUNTANT_sendagreement`.

---

## `email_templates`

Editable subject/body for outbound automation emails. Pipeline-scoped.

| Column | Type | Notes |
|---|---|---|
| `id` | integer | pk |
| `pipeline` | text | not null. Pipeline key (e.g., `"map1"`). |
| `template_name` | text | not null. Template key (e.g., `"final_decision"`, `"payment_email"`, `"ceo_countersign"`). |
| `subject` | text | |
| `body` | text | HTML body with placeholder tokens. |
| `to_list` | jsonb | nullable. **Added 2026-06-15; generalized 2026-07-03.** The email's To config — an array mixing raw emails and UPPERCASE role tokens (`CLIENT`, `MEMBER`, `ASSIGNED_PF`, `SPECIALIST`, `ADVISOR`, `ACCOUNTANT`, `TEAM_MEMBER`, `TEAM`, `RECIPIENT`), resolved per-send by `utils/email-recipients.ts resolveTemplateRecipients()`. Same token support applies to `cc_list`/`bcc_list`. Seeded for all rows by migration `email_templates_recipient_seeds`; edited as chips in the Email Templates tab; `automation_save_email_template` sanitizes (emails validated, known tokens kept). Empty/unresolvable To falls back to the handler's built-in target. **⚠️ A role token here does NOTHING unless the SENDING HANDLER passes that key in its `resolveTemplateRecipients` ctx** — an unresolved token is skipped **silently**, so a row can look correctly configured and Cc nobody (gotcha **#324**; the `CLIENT_PAYMENT_CONTINUATION` rows 172/209 did exactly that until v696). To know who actually receives an email, read the handler's ctx, not this column. **Blanket internal Cc (2026-07-31, addresses corrected 2026-08-03; DML only):** every row in pipelines `MAP 1` (34), `TAX` (59 — both tax programs), `PIP` (5) and `PARTNERSHIP_FAST_TRACK` (10) — **108 rows, 108/108** — carries **`tnmiller@vfo-services.com`** + **`tvaldes@vfo-services.com`** in `cc_list`, with zero `@elitert.com` Tracy/Tray leftovers there. **45 rows OUTSIDE those pipelines deliberately KEEP `@elitert.com`** (`SPECIALIST_ONBOARDING` 27, `VFO_SPECIALIST_REVENUE` 9, `MEMBER_MEMBERSHIP_FEES` 8, `SPECIALIST_LICENSE_CONTINUATION` 1) — the split is per-pipeline and intentional, so do not unify it (gotcha **#326**). **A NEW template in those pipelines does not inherit the Cc** — it lives in the row, so seed the pipeline-correct address explicitly. The typo address `tracy@vfo-services.com` (never existed) was removed from ids 10/115/25/137 in the 2026-07-31 pass and now appears nowhere. **`bcc_list` footnote:** 181 rows still carry `aanderson@elitert.com` / `platham@elitert.com`; the nine auto-send rows below were stripped to `jlatham@elitert.com` only. See [integrations/gmail.md](../integrations/gmail.md). |
| `active` | boolean | default `true` |
| `send_mode` | boolean | not null, default `false`. **Added 2026-07-03** (migration `email_templates_send_mode`). The per-template **Draft/Send toggle**: `false` = the email is only created as a Gmail draft (original behavior); `true` = the draft is auto-sent via Gmail `drafts.send` right after creation (`utils/email-delivery.ts`). Edited per-email or per-section ("All draft / All send") in the Email Templates tab; `automation_save_email_template` accepts `send_mode` and a bulk `{ ids: [...], send_mode }` form. See gotcha #181. **⚠️ As of 2026-09-04 exactly SEVENTEEN rows are `true` and every other row is Draft — derive it, never trust this number (`select count(*) from email_templates where send_mode`), and see the auto-send roster below (gotcha #325).** *(Derived 2026-09-18: **49** — the roster table below is the original seventeen and is not the whole set; the growth is almost entirely the two onboarding pipelines, 19 rows each. The five 2026-09-17 tax intake rows are all `false`; `TAX` still has exactly one auto-send row, 200.)* ⚠️ The flag is only consulted if the `(pipeline, template_name)` pair passed to `draftGmail`'s `sendMode` (or to `gmailDraftFetch`) **matches the row the handler actually loaded** — a mismatch silently leaves the mail in Drafts (gotcha **#356**). |
| `created_at` | timestamptz | default `now()` |

**The auto-send roster — the ONLY seventeen rows with `send_mode=true` (2026-08-03, DML only; gotcha #325; ids 215 and 216 added 2026-08-10; ids 247-249 and 254-256 added 2026-09-04).** Every other template in the table is Draft-only, so these seventeen are the complete list of emails that leave without a human reading them first:

| id | pipeline | template_name |
|---|---|---|
| **158** | `LOGIN_SETUP` | `MANUAL_login_setup` — **shared by FOUR login types** (member / specialist / client / tax planner); the only one of the nine with **no Cc at all** |
| **159** | `MEMBER_PAYOUT` | `member_connect_setup` |
| **167** | `VFO_SPECIALIST_REVENUE` | `SPECREV_connect_setup` |
| **182** | `SPECIALIST_PAYOUT` | `specialist_connect_setup` |
| **183** | `STRATEGIC` | `strategic_group_connect_setup` |
| **190** | `MEMBER_MEMBERSHIP_FEES` | `MEMBERSHIP_setup_link` |
| **191** | `MEMBER_MEMBERSHIP_FEES` | `MEMBERSHIP_transfer_setup_link` |
| **200** | `TAX` | `tax_planning_group_connect_setup` |
| **210** | `VAULT` | `VAULT_request_documentation` — **shared by THREE entity types** (client / member / specialist) |
| **215** | `MEMBER_MEMBERSHIP_FEES` | `MEMBERSHIP_transfer_setup_link|annual` — added 2026-08-10 (gotcha #351) |
| **216** | `LOGIN_SETUP` | `password_reset` — added 2026-08-10; the self-service "forgot passcode" link. **The first auto-send row whose sender is a PUBLIC handler** (`request_password_reset`), so `send_mode=false` here is not "slower", it is a silent outage for four portals (gotchas #355 / #356). No Cc. |
| **247** | `ADVISOR_ONBOARDING` | `ADVISOR_meeting_reminder` — added 2026-09-04; the preliminary-meeting reminder, 1 business day out |
| **248** | `ADVISOR_ONBOARDING` | `ADVISOR_meeting_reminder_60m` — added 2026-09-04; 60 minutes out, **confirmed meetings only** |
| **249** | `ADVISOR_ONBOARDING` | `ADVISOR_meeting_reminder_10m` — added 2026-09-04; 10 minutes out, **confirmed meetings only** |
| **254** | `ACCOUNTANT_ONBOARDING` | `ACCOUNTANT_meeting_reminder` — added 2026-09-04 |
| **255** | `ACCOUNTANT_ONBOARDING` | `ACCOUNTANT_meeting_reminder_60m` — added 2026-09-04 |
| **256** | `ACCOUNTANT_ONBOARDING` | `ACCOUNTANT_meeting_reminder_10m` — added 2026-09-04 |

**The six 2026-09-04 additions break the roster's old theme and did so deliberately** — they are a COUNTDOWN (1 business day / 60 min / 10 min before a preliminary meeting), fired by `onboarding-meeting-reminder-sweep-5min`, not a "here is your link" email. A reminder that waits in Drafts until someone notices it is not a reminder; the justification is #356's, one step further. They obey the `ADVISOR_ONBOARDING` / `ACCOUNTANT_ONBOARDING` sandbox toggles like every other email (#324). The other eleven are "here is your link, go do the thing" emails; **nothing carrying money, a decision, an agreement or a receipt was flipped**, including the two `CLIENT_PAYMENT_CONTINUATION` setup-link rows (ids 172/209), which stay Draft on purpose. Uniform recipient shape: **Bcc = `jlatham@elitert.com` only** (Aaron + Paul removed from these eleven only) and **Cc = `tnmiller@vfo-services.com` + `tvaldes@vfo-services.com`** on all but 158 and 216. **Editing one of these eleven reaches a real person on the next trigger with nobody in between**, and the two shared rows have a wider blast radius than their name suggests.

**Password-reset template (2026-08-10, migration `20260810110000_login_setup_password_reset_template.sql`):** one row **id 216**, `pipeline='LOGIN_SETUP'` / `template_name='password_reset'` — the second `LOGIN_SETUP` row, and the only template drafted by an **unauthenticated** handler. `to_list '["RECIPIENT"]'` (the account holder, resolved from the `*_logins` row the handler found — never from the request body), **`cc_list '[]'`** (a reset link should not be copied to anyone, same reasoning as 158), `bcc_list '["jlatham@elitert.com"]'`, `active=true`, **`send_mode=true`**. Placeholders `[FIRST_NAME]` / `[NAME]` / `[RESET_LINK]` (an inline-styled anchor to `${PORTAL_BASE}/set-password?token=`, label "Reset my passcode", matching `send-email.ts`'s `buttonHtml` style); the body states the **1-hour** expiry and that ignoring the email leaves the current passcode working. Listed in the Email Templates tab under **Miscellaneous**, immediately after `MANUAL_login_setup`. Because the handler always reports success to the caller (#355), **nothing surfaces a delivery failure here** — Drafts is the only place a problem shows up.

**TAX member-pays + meeting templates:** 22 member-variant rows (**ids 126–147**) were added, each = an existing TAX client template name + the suffix ` (member signing/paying on clients behalf)` — selected when `client_tax_plans.member_paying_on_behalf=true`. Plus **`TAX_highlevelmeeting_confirm|Yes`** (**id 148**, CLIENT-ONLY — no member variant; the body's planner name is the `[Planner Name]` token, de-hardcoded from "Tim Gacsy" 2026-07-22) used by `automation_TAX_highlevelmeeting_confirm`. The old `TAX_meeting_nudge|Yes` (id 42) and `TAX_implementation_announce|Yes` (id 28) orphan rows were DELETED 2026-07-03 at Jake's direction — no code referenced either (the daily nudge email was replaced by an in-app notification; the implementation announcement was never wired up).

**Hardcoded-email ports (2026-07-04, migration `email_templates_hardcoded_ports`):** 11 rows covering the formerly hardcoded emails — `CONTRACT_member_revshare|first/subsequent` + `CONTRACT_installment_charge_failed` (MAP 1), `TAX_member_revshare|retainer/implementation` + `TAX_implementation_charge_failed` + `TAX_deposit_refund` (TAX), `PIP_member_revshare` (PIP), `specialist_connect_setup` (new pipeline `SPECIALIST_PAYOUT`), `strategic_partner_revshare` + `strategic_group_connect_setup` (new pipeline `STRATEGIC`). Bodies are verbatim ports with block placeholders (`[SCHEDULE_BLOCK]`, `[REVSHARE_BLOCK]`, `[STATUS_BADGE]`, `[PAY_BUTTON]`, `[SETUP_LINK]`, …) that the handlers fill per-send. Supersedes gotcha #163.

**Strategic partner share is addressed to the PARTNER COMPANY (2026-08-11, migration `20260811150000_strategic_revshare_partner_subject_cc.sql`):** row **id 178** (`pipeline='STRATEGIC'` / `template_name='strategic_partner_revshare'`) had its **`subject`** changed from `[Member Name]` to **`[Partner Name]`** — it now reads *"VFO Services - Revenue Share Confirmation - [Partner Name]: [Client Name] ([Client Ref])[Subject Suffix]"*, i.e. "Action Coach: Deanna Haines (20028-001) - Payment 2 of 4" — and its **`cc_list`** from `[]` to **`["tnmiller@vfo-services.com","tvaldes@vfo-services.com"]`** (Tracy + Tray, matching `CONTRACT_member_revshare|*`). `to_list` stays **`[]`**, which is deliberate: `resolveTemplateRecipients` falls back to `ctx.RECIPIENT`, and `draftStrategicPartnerEmail` passes the partner's `strategic_member_groups.contact_email` there — so the To address is always resolved server-side from the group row, never from a template list. `bcc_list` (Anna + Paul), `body`, `active` and **`send_mode=false`** are unchanged. **This is ONE row shared by BOTH the MAP 1 and Tax Planning strategic emails** — every caller of `draftStrategicPartnerEmail` reads the same pipeline/name pair — so an edit here lands on Tax Plan IQ as well as Action Coach (gotcha #364). The change needed **no deploy**: `[Partner Name]` was already substituted by `utils/strategic-partner-email.ts` in live v718. The **body was deliberately left alone** — it still carries "Member name" and "Member number" rows in the payment-details table, so the connected member is still identified inside the email even though the subject now names the partner.

**Member revenue-share HELD notice (2026-08-24, migration `20260824120000_member_revshare_hold_seeds.sql`)** *(v: 2026-08-24)*: one row **id 230**, `pipeline='MEMBERS'` / `template_name='MEMBER_revshare_held'` — **the only `MEMBERS`-pipeline row**, and an **internal** notice, not a member-facing one. `to_list '["platham@elitert.com"]'` (Paul), `cc_list '["aanderson@elitert.com","tvaldes@elitert.com","tnmiller@elitert.com"]'` (Anton + Tray + Tracy, `@elitert.com` — matching the non-`MAP 1`/`TAX`/`PIP`/`PFT` split of #326, so do **not** unify it to `@vfo-services.com`), `bcc_list '[]'`, `active=true`, **`send_mode=false`**. Six tokens: `[Member Name]` `[Member Number]` `[Member Status]` `[Revenue Type]` `[Source]` `[Amount]`. Drafted by `utils/member-payout-hold.ts` `draftMemberHeldNotice` the first time a member revenue-share leg is held because the member is suspended or paused (all four engines: MAP 1, Tax, PIP, Specialist Revenue). **Two properties differ from a normal template row.** (1) **The caller passes no `sendMode` at all**, so the Draft/Send toggle can never reach it — this one is structurally draft-only, not draft-by-configuration. (2) **The code carries a hardcoded fallback subject/body and To/Cc lists**, used if the row is missing or inactive: a missing template must never block the hold itself. *(A post-deploy edit removed one sentence from the seeded body and the fallback alike, so the fallback in **deployed v778** still carries it — the DB row governs live behaviour, and the two re-converge on the next backend deploy.)* **It IS admin-editable**, listed in the Email Templates tab under **Miscellaneous** via an explicit `['MEMBERS','MEMBER_revshare_held']` item in that section's list — the same pattern as `['VAULT','VAULT_request_documentation']`, not a new whole-pipeline section — with its `templateMeta.js` description and a *"To: Paul · Cc: Anton, Tray, Tracy"* recipients hint.

**New Model Sale team template (2026-06-15):** one row **id 155**, `pipeline='TEAM'` / `template_name='new_model_sale'` — the only `TEAM`-pipeline row and the only consumer of the `to_list` column. It's an internal notification drafted by `utils/new-model-sale-email.ts` (fired from `automation_ADVISOR_createmember` / `automation_ACCOUNTANT_createmember` as the final onboarding step), addressed `To` the `to_list` team members. Body placeholders are filled from the `advisor_onboarding`/`accountant_onboarding` row (member name/number/email, agreement name, selected plans, payment plan, invoice number, plus the `sale_closer`/`sale_setter`/`sale_introduced_by`/`sale_company_name`/`sale_website` sale-attribution fields). In sandbox the draft goes only to the pipeline's `sandbox_email`.

**Vault "Request documentation" template (2026-07-30, migration `20260730120000_vault_request_docs.sql`):** one row, `pipeline='VAULT'` / `template_name='VAULT_request_documentation'` — the only `VAULT`-pipeline row, and the ONE template serving all three entity types (client / member / specialist) across both vault sections. `to_list '["RECIPIENT"]'`, **`cc_list` = `MEMBER` + `tnmiller@vfo-services.com` + `tvaldes@vfo-services.com`**, `bcc_list '["jlatham@elitert.com"]'`, `active=true`, and **`send_mode=true` as of 2026-08-03 — this row now AUTO-SENDS** (it was seeded Draft; see the auto-send roster above and gotcha #325). Every documentation request therefore leaves immediately, for all three entity types, with no admin review step. Placeholders `[Recipient Name]` / `[Recipient First]` / `[REQUESTED_INFO]` (the admin's typed request, `\n`→`<br>`, stored NOWHERE) / `[UPLOAD_BUTTON]` (an inline-styled anchor to `${PORTAL_BASE}/vault-upload?token=`, label "Upload Documents"). **The `MEMBER` Cc token is filled ONLY for client requests** — `vault_request_docs` passes a `MEMBER` ctx only when `entity_type==='client'`, so on member/specialist requests the role token resolves to nothing and the Cc list comes back empty. An unset role token drops silently, so reusing this template for a new entity type means deciding its `MEMBER` ctx explicitly (gotcha #310). Listed in the Email Templates tab under **Miscellaneous** (an explicit `['VAULT','VAULT_request_documentation']` item in the `misc` section's list, not a new section). _Seeded during the v681 session; the live row's `cc_list` was corrected from a raw address to `["MEMBER"]` post-apply and the migration file was updated to match._ **GATED ON THE CLIENT/SENSITIVE SURFACE since 2026-08-13 (#393):** `ClientVaultTab.jsx` replaces the *"Request documentation"* button with a locked warning while any `status='live'` tax plan has a NULL `tax_returns_received_at` — the tracked `/tax-upload` token minted by the Tax 1 *"Request Tax Returns"* step is what stamps that column, and an untracked second route would bypass it. **Frontend-only: `vault_request_docs` is unchanged and still reachable**, and the member / specialist / planner-panel vault surfaces are untouched (the button is rendered directly in `ClientVaultTab`, not through `VaultSections`).

**Card-update template (Phase D):** one row **id 156**, `pipeline='PAYMENTS'` / `template_name='card_update'` — the only `PAYMENTS`-pipeline row. The admin-initiated payment-method-change email; placeholders `[First Name]` / `[UPDATE_LINK]` (the `/update-card?token=…` link backed by `card_update_tokens`, see [pipeline.md](pipeline.md)).

**Touched by:** `automation_load_email_templates`, `automation_save_email_template`, plus every `automation_*` handler that sends a Gmail draft (it pulls subject/body from this table). Frontend: [EmailTemplatesPanel.jsx](src/components/admin/EmailTemplatesPanel.jsx).

---

## `document_numbers`

Append-only sequence ledger. Each row reserves a number-of-type for a client/advisor/accountant, used as the source of truth for `pipeline_map1.invoice_number`, `pipeline_map1.rec1..rec4_number`, `advisor_onboarding.invoice_number`, `accountant_onboarding.invoice_number`, etc. — so that numbers are unique and stable even across retries.

| Column | Type | Notes |
|---|---|---|
| `id` | integer | pk |
| `type` | text | not null. e.g., `'invoice'`, `'receipt'`. |
| `number` | text | not null, **UNIQUE**. The formatted number. MAP1: `INV-{clientRef}-{seq:0004}` / `REC-{clientRef}-{seq:0004}`. Tax (retainer + implementation): `INV-{clientRef}-{seq:0004}` / `REC-{clientRef}-{seq:0004}`. Advisor: `INV-ADV{onboarding_id}-{seq}` / `REC-ADV{onboarding_id}-{seq}`. Accountant: `INV-ACC{id}-{seq}` / `REC-ACC{id}-{seq}`. PIP: `INV-PIP-{clientRef}-{seq}` / `REC-PIP-{clientRef}-{seq}`. Specialist bg: `INV-SPEC-{id}-{seq}` / `REC-SPEC-{id}-{seq}`. Specialist license: `INV-SPECLIC-{id}-{seq}` / `REC-SPECLIC-{id}-{seq}`. The starting `seq` is a **global** count of that type for invoices (and the onboarding/PIP receipts), but **per-owner** for MAP 1 + Tax *receipts*. |
| `client_id` | integer | nullable. Soft reference to `clients.id`. Used by MAP1, Tax, and PIP rows. |
| `advisor_onboarding_id` | bigint | nullable FK → `advisor_onboarding(id)` `ON DELETE SET NULL`. Used by advisor invoice/receipt rows. |
| `accountant_onboarding_id` | bigint | nullable FK → `accountant_onboarding(id)` `ON DELETE SET NULL`. Used by accountant invoice/receipt rows. |
| `specialist_onboarding_id` | bigint | nullable FK → `specialist_onboarding(id)` `ON DELETE SET NULL`. Used by specialist background-check (`INV/REC-SPEC`) + monthly-license (`INV/REC-SPECLIC`) rows. |
| `created_at` | timestamptz | default `now()` |

**Constraints + allocation (collision-safe since 2026-06-09):**
- `UNIQUE(number)`.
- `document_numbers_exactly_one_owner` CHECK requires **exactly one** of {`client_id`, `advisor_onboarding_id`, `accountant_onboarding_id`, `specialist_onboarding_id`} non-null. Widened by migration `document_numbers_widen_owner_check` — the OLD CHECK only allowed client/advisor, so **every accountant/specialist insert silently failed** (the seq was computed but never recorded → numbers could be re-used). When inserting, stamp exactly one owner FK.
- All allocation goes through **`utils/doc-numbers.ts` `allocateDocNumber()`** which bumps the seq and retries until the UNIQUE insert succeeds — fixing the old `count → build → insert-once → ignore-error` pattern that silently stalled the counter and reused a number on collision (gotcha #92). Don't revert to insert-once.

**Touched by (all via `allocateDocNumber`):** `automation_CONTRACT_invoicereceipt`, `automation_TAX_invoicereceipt`, `automation_TAX_implementation_receipt`, `automation_ADVISOR_invoicereceipt`, `automation_ACCOUNTANT_invoicereceipt`, `automation_PIP_invoicereceipt`, `automation_SPECIALIST_bgreceipt`, `automation_SPECIALIST_licinvoicereceipt`, and — **2026-09-17** — `utils/tax-deposit-docs.ts` (the $500 tax intake deposit pair, called from `utils/tax-intake-finalize.ts`, not from a dispatched action).

**The tax intake deposit pair (2026-09-17) uses the SAME two sequences as the retainer pair** — `type='invoice'`, owner `client_id`, `INV-{clientRef}-{seq:0004}` from the **global** invoice count, and `type='receipt'`, owner `client_id`, `REC-{clientRef}-{seq:0004}` **per owner** — so a client who pays a deposit and later a retainer holds `REC-<ref>-0001` for the deposit and `-0002` for the retainer, while the two invoices are consecutive only by accident. The numbers are stamped on `tax_intake_requests.deposit_invoice_number` / `deposit_receipt_number` BEFORE the PDF render and latched by `deposit_docs_at`; **the reservations are deliberately NOT deleted when a test fixture is wiped** (the 2026-09-18 clean-up left `INV-59524-008-0062` / `REC-59524-008-0001` in this ledger), because a freed number would be re-issued to a real document. See [tax.md](tax.md#tax_intake_requests-added-2026-09-17).

**Revised tax invoice pair (2026-09-15, migration `20260915140000_tax_amended_invoice.sql`):** rows **269** `TAX_amended_invoice_email` and **270** `TAX_amended_invoice_email (member signing/paying on clients behalf)`, pipeline `TAX`, `send_mode=false`, recipients copied from 25 / 137 with Bcc Anton + Paul + Dan. **The subject is IDENTICAL to 25 / 137 on purpose** — Gmail threads the draft onto the original invoice email — so do not "fix" it to say Revised. Tokens: `[Client First]` / `[Member First]` / `[Client Name]` / `[Member Name]` / `[Total Fee]` (revised) / `[Original Total]` / `[Payment Amount]` (received so far) / `[Invoice Number]`. Rendered by `actions/tax/amended-invoice.ts`. The same day rows **27 / 141** (`TAX_refund_email|Yes` + twin) dropped the word *"full"* so a partial refund's email is not a lie (migration `20260915123000`).

**No-fee implementation acknowledgement pair (2026-09-17, migration `20260917150000_tax_impl_proceed_no_fee_templates.sql`):** rows **273** `TAX_implementdecision|Proceeding|no-fee` and **274** `TAX_implementdecision|Proceeding|no-fee (member signing/paying on clients behalf)`, pipeline `TAX`, `send_mode=false` (Draft, like every sibling on this path). Recipients copied **to the entry** from **211** / **246** — 273: To `CLIENT`, Cc `MEMBER` / `ASSIGNED_PF` / `TAX_PLANNER` / Tracy / Tray, Bcc Anton + Paul; 274: To `MEMBER`, Cc `CLIENT` and the same rest. **They carry NO `[Payment Amount]` token and no discount footnote** — that is the whole reason they exist: on a plan whose implementation fee was amended to `$0.00`, 211 / 246 would have told the client *"$0.00 will now be processed"*, so the arm drafted nothing and the client who clicked Proceed received silence for eight days (#509). Tokens: `[Client First]` / `[Client Name]` / `[Member First]` / `[Member Name]` / `[PF Name]`; the handler appends `VFO_SIGNATURE`, exactly as it does for 211. Rendered by `actions/tax/implement-final-decision.ts` (the `no_fee_due` arm). Wording: the retainer already covers the full engagement, nothing further is due, no payment will be taken.

**Tax intake templates (2026-09-17/18, migrations `20260917120100_tax_intake_email_templates.sql`, `20260917200000_tax_intake_client_route.sql`, `20260917210000_tax_intake_deposit_docs.sql`, `20260918140000_tax_deposit_refund_subject.sql` — unit 1 of [DIRECT to Tax Planning](../plans/direct-tax-planning/README.md), flow in [flows/tax-intake.md](../flows/tax-intake.md)):** five NEW `TAX` rows, **all `send_mode=false`** (Draft — including 277, which means a member's "Send link" only creates a Gmail draft until Jake flips it), all Bcc Anton + Paul, all with the pipeline's Tracy + Tray `@vfo-services.com` Cc seeded explicitly (#326), and one re-addressed row:

| id | template_name | To | Cc | Drafted by | Tokens |
|---|---|---|---|---|---|
| **275** | `TAX_new_case_confirmation` | `MEMBER` | Tracy, Tray | `utils/tax-intake-finalize.ts` when `payer='member'` (route A) or the deposit was waived | `[Member First]` `[Client Name]` `[DEPOSIT_LINE]` — the *"we also received the $500 deposit"* sentence, EMPTY on a waived intake |
| **276** | `TAX_intake_request\|holistic` | `MEMBER` | `ASSIGNED_PF`, Tracy, Tray | `utils/tax-intake-request-email.ts` after a Holistic client's FIRST MAP 1 payment settles (once, latched on `clients.tax_intake_requested_at`) | `[Member First]` `[Client Name]` `[BUTTONS]` — the green *"Complete the Tax Planning Form"* button onto `/member?tab=msm_tax&intake_client=<id>` |
| **277** | `TAX_intake_link` | `CLIENT` | `MEMBER`, Tracy, Tray | `utils/tax-intake-link-email.ts` from `tax_intake_send_link` (route B; a re-send re-drafts with the SAME token) | `[Client First]` `[Member Name]` `[DEPOSIT_SENTENCE]` (empty when the member's waiver already applies at send time) `[BUTTONS]` onto `/tax-intake?token=<intake_token>` |
| **278** | `TAX_new_case_confirmation\|client` | `CLIENT` | `MEMBER`, Tracy, Tray | `utils/tax-intake-finalize.ts` when `payer='client'` | `[Client First]` `[Client Name]` `[Member Name]` `[DEPOSIT_LINE]` — *" and your $500 deposit, for which we attach a receipt."* when money moved, a bare `.` when not, so the sentence closes either way |
| **279** | `TAX_deposit_refund\|client` | `CLIENT` | `MEMBER`, `ASSIGNED_PF`, Tracy, Tray | `actions/tax/deposit-refund.ts` when the plan's newest intake row says the client paid | 181's tokens, the client addressed and the deposit described as theirs |
| **181** | `TAX_deposit_refund` (existing) | **`MEMBER`** (was `CLIENT`) | `ASSIGNED_PF`, Tracy, Tray | `actions/tax/deposit-refund.ts` for `payer='member'` **and for a plan with NO intake row** (a hand-pasted deposit — Jake's call); falls back to the client's address, logged, when the member has no email | `[Member First]` (was `[Client First]`) `[Refund Reason]` `[Refund Amount]` `[Client Name]` — *"the deposit you paid for [Client Name]"* |
| **NEW 2026-09-21** | `TAX_stop_no_deposit` | `CLIENT` | `MEMBER`, `ASSIGNED_PF`, Tracy, Tray | `actions/tax/stop-no-deposit.ts` (`automation_TAX_stopnodeposit` / `tax_direct_stop_no_deposit`) when a **waived** plan is stopped from the Tax Plan Red Light step | `[Client First]` `[Client Name]` `[Member First]` `[Stop Reason]`. Subject `VFO Services - Tax Planning Update - [Client Name]`. Column shape copied verbatim from 279 (same house HTML wrapper, same lists, `send_mode=false`, **no in-body footer**). The typed reason opens the second paragraph and the fixed *"No deposit was taken, so there is nothing to refund."* closes it. Migration `20260921120000_tax_stop_no_deposit_template.sql` |

Three body/subject rules from those migrations, each a guarded `replace()` so a re-run is a no-op and a hand-edited row is left alone:

- **`[RECEIPT_BUTTON]` is GONE from 275 and 278** (docs migration). It used to expand to a green *"View receipt"* button onto the Stripe-hosted receipt; the deposit now carries its own house **invoice + receipt PDF pair** as attachments (the house rule for every payment — never a Stripe receipt link; plan decision 17), so the handler substitutes nothing and the bodies no longer carry the token. The paragraph break was kept.
- **The in-body AI-PC sign-off block was stripped from SIX revshare templates** — `TAX_member_revshare|retainer`, `TAX_member_revshare|implementation`, `TAX_planner_revshare|retainer`, `TAX_planner_revshare|implementation`, `STRATEGIC/strategic_partner_revshare` and `PIP/PIP_member_revshare` (that one also lost the bare *"Regards"* paragraph that only introduced the block). **0 bodies carry it live.** Reason: the footer is now appended by the DELIVERY layer (`utils/email-signature.ts`, in `draftGmail` / `gmailDraftFetch` / `deliverRaw` — see [integrations/gmail.md](../integrations/gmail.md#the-delivery-layer-footer-guard-2026-09-17)), so a body carrying its own block would have shipped TWO footers. 181's body lost its block + *"Best regards,"* line for the same reason, and none of the five new bodies carries a footer by design.
- **Subject of 181 AND 279 is `VFO Services - Tax Planning Deposit Refunded - [Client Name]`** (2026-09-18, applied live by `execute_sql` the same day and committed as `20260918140000`). Jake: the client's name goes at the END after a dash, matching the house `" - Name"` subject shape — not in parentheses, which is how 279 was first seeded and how 181's old subject read.

**Recipient contract reminder (#324):** every role token above is fed by its handler's `resolveTemplateRecipients` ctx — `ASSIGNED_PF` on 276 / 181 / 279 resolves through the four-name `pf-emails.ts` map, so a client whose PF is not in it gets no PF Cc silently. Sandbox drops every Cc, so the To/Cc shape above was verified from the `sandbox: would have sent` log line, not from a delivered mail; **no real (non-sandbox) send of any of the six has happened yet.**
