# Notifications

In-portal notification feed. The bell icon at the top of `AdminPortal` AND the admin variant of `/admin/client/:id` ([NotificationBell.jsx](src/components/NotificationBell.jsx)) reads from `notifications`. **As of 2026-07-03 every insert routes through `utils/notify.ts notifyByRule(supabase, ruleKey, {...})`** — the `notification_rules` row for that key can override recipients, disable the notification, or (sweep tiers) change the delay. **One documented exception (2026-07-10): the personal-reminder sweep inserts directly** (user-authored reminders have no rule key — gotcha #213). Full per-notification audit: [NOTIFICATION_AUDIT.md](../NOTIFICATION_AUDIT.md). Gotchas #176–#180, #212–#213.

**Full-page Notifications view (2026-07-10):** the bell's **View all** opens `NotificationsPage.jsx` (admin portal `activeTab='notifications'`, `/admin?tab=notifications` — available to EVERY admin, not tab-granted). Two scopes — **Current** (unread) and **Archive** (read; retained 90 days then hard-deleted by the daily `automation_NOTIFICATIONS_purge` cron, jobid 15) — each filterable by kind (All / Action required / FYI / Reminders), newest/oldest sort, 50-per-page pagination, and checkbox bulk clear (`mark_notifications_read`, dismissible + recipient-scoped rows only). Reminder rows (`pipeline='REMINDER'`) have a reserved violet left bar + REMINDER pill in both the bell and the page. "Clearing" anywhere only flips `read=true` — rows are deleted only by the purge cron.

## `notifications`

| Column | Type | Notes |
|---|---|---|
| `id` | integer | pk |
| `recipient` | text | not null. Routing key. `actions/notifications/load.ts` filters `recipient = session.email OR 'admin' OR 'all'` — so a value is any login email (member OR admin; e.g. `tnmiller@elitert.com` for Tracy on SPECIALIST notifications, gotcha #60), `'admin'` (all admins), or `'all'`. **A `tax_planner` or `member` session is scoped to `recipient = session.email` ONLY** — the broadcasts never reach them (#259). A recipient email with no matching login row is stored but never shown. Recipient resolution (assigned-PF / team-member / assigned-admin lookups, plus any Notification Editor override) happens inside `notifyByRule` before insert. **Since 2026-09-21 a recipient may be a MEMBER's `member_logins.email`** — on a Direct tax plan the client's PF *is* the member, so `utils/tax-notify.ts resolveTaxPf` addresses every PF bell to them and `links` carries their `/member/client/<id>` route. |
| `client_id` | integer | fk → `clients.id` (NO ACTION) |
| `pipeline` | text | Pipeline name (e.g., `"MAP 1"`). Rendered as a chip in the bell row. |
| `title` | text | not null. The bell row shows ONLY the title (single line, ellipsized) — write who+what into it. |
| `message` | text | **Rendered INLINE in the bell row, in full, on wrapping lines** (`white-space:pre-wrap`, `overflow-wrap:anywhere`) as of 2026-08-11 — it is no longer hidden in a hover tooltip. That is what lets an action-required bell carry a real instruction (e.g. *complete the "Additional information required" step*) instead of a title fragment. Write the instruction here; keep the **title** stable, because titles are the clearing key (see `dismissible` below). |
| `link` | text | Frontend route the bell navigates to on row click (must be real — gotcha #18, **and real *for that recipient's portal***). `notifyByRule` accepts an optional `links` map (lowercase recipient email → link) applied per row, so one event can give a tax planner `/tax-planner/client/<id>?program=<n>` while admins keep `/admin/client/<id>?tab=tax` — gotcha #292. |
| `read` | boolean | default `false`. Status field. |
| `dismissible` | boolean | default `true`. `true` = FYI (green Done button; row click also dismisses). `false` = action-required: NO Done button, sorted to the top with an orange ACTION pill, excluded from "Mark all read", and **`mark_notification_read` refuses it server-side (gotcha #179)** — it clears only when the completing handler (e.g. `automation_TAX_pricing`, `create-member`, reviewer-notes saves, `clearJakeFailure`) writes `read=true` directly. **This column, set from the CALL SITE's `dismissible:false`, is the only thing that makes a bell action-required** — `utils/notify.ts` deliberately does not let a `notification_rules` row override it (see `action_required` below). Where several such bells can coexist for one client the completing handler matches on **pipeline + `client_id` + `dismissible=false` + `read=false` + `title ILIKE '<prefix>%'`**, which makes those titles load-bearing; the tax track's map of prefix → clear site lives in `utils/tax-review-bell.ts`. |
| `created_at` | timestamptz | default `now()` |

**Touched by:** `load_notifications`, `mark_notification_read` (FYI-only), `load_notifications_page` (paginated, incl. read rows), `mark_notifications_read` (bulk FYI clear), `automation_REMINDER_sweep` (direct insert — the documented #176 exception), `automation_NOTIFICATIONS_purge` (deletes read rows >90d). All other inserts ONLY via `notifyByRule` (raw inserts are a regression — gotcha #176).

**Handlers that mark rows read directly — the `dismissible=false` exception (v: 2026-08-19).** `mark_notification_read` / `mark_notifications_read` are FYI-only **by design**: an action-required row can be retired only by the handler that completes the work it names (**#365**). Those completing handlers write `read=true` themselves, matched on **title prefix**, which is why a bell title is an API and not cosmetic. Current direct clearers: the five prefix clears in `utils/tax-review-bell.ts` (incl. `clearPresentationDownloadBells`, now also reached by the **new** `tax_presentation_downloaded`); `automation_PCADMIN_pricing` / `automation_PCADMIN_extrameeting` (client-wide, `dismissible=false` only); and — **new 2026-08-19** — `automation_stall_ack`, the first clearer that ignores `dismissible` entirely and sweeps FYI and action-required rows for its step alike. Scoping and title literals: [../flows/notifications.md](../flows/notifications.md); gotcha **#418**.

## `personal_reminders` *(new 2026-07-10; deny-all RLS)*

Self-scheduled admin reminders (Notifications page → Reminders sub-tab). Per-admin: `reminder_create`/`reminder_load`/`reminder_delete` are ADMIN_ONLY and always scoped to `auth.session.email` (never a body email). Delivered by `automation_REMINDER_sweep` (cron `reminder-sweep-5min`, jobid 14) as a dismissible bell row (`pipeline='REMINDER'`, link `/admin?tab=notifications`) within ~5 minutes of `fire_at`.

| Column | Type | Notes |
|---|---|---|
| `id` | bigint | pk, identity |
| `recipient_email` | text | not null — the authoring admin's login email (also the bell recipient) |
| `message` | text | not null, max 500 chars (validated in `reminder_create`) |
| `fire_date` / `fire_time` | date / text | The chosen wall-clock (`HH:MM` 24h) in the chosen zone — kept for display |
| `timezone` | text | IANA zone (validated via `Intl.DateTimeFormat`) |
| `fire_at` | timestamptz | The resolved absolute instant — computed in Deno by `utils/timezone.ts zonedTimeToUtc` (two-pass Intl offset technique, DST-safe); must be in the future at create time |
| `fired_at` | timestamptz | null = upcoming (cancellable); stamped ONLY after a successful bell insert so transient failures retry |
| `created_at` | timestamptz | default `now()` |

## `notification_rules` *(new 2026-07-03; deny-all RLS)*

One row per notification (and per sweep reminder-email tier), edited in **Admin Portal → Automation → Notification Editor** (`NotificationEditorPanel.jsx`; actions `notification_rules_load` / `notification_rules_save`, ADMIN_ONLY + `TAB_ACTIONS.automation`). Edited over time across 10+ areas. **NEW 2026-07-22 (migration `20260722130000`, +8 rows):** a new **`Tax Planners`** area with SIX planner bells — `TAX_planner_allocated`, `TAX_planner_returns_uploaded`, `TAX_planner_hlm_ready`, `TAX_planner_post_meeting`, `TAX_planner_decision2_ready`, `TAX_planner_impl_decision_ready` — seeded with `default_recipients '["ALLOCATED_TAX_PLANNER"]'` as a PLACEHOLDER (**`TAX_planner_hlm_ready` is the exception since 2026-08-17** — migration `20260818000100_hlm_confirm_bell_to_tray.sql` moved it, and the later `TAX_roi_skipped_hlm_ready`, to `'["tvaldes@elitert.com"]'` because the "Detailed tax plan meeting confirmation email" step became Tray's; `invoice-receipt.ts` / `skip-roi-meeting.ts` now call `notifyByRule` with that `defaultRecipients` rather than `notifyAllocatedPlanner`. Key, area and **bell title are unchanged** — the title is the clearing API, and leaving it alone is what let the re-address skip a cutover, #411) (the real planner email is resolved per-call from the plan's `tax_planner_id` and passed directly by `notifyAllocatedPlanner`, `utils/tax-planner-notify.ts`); PLUS two `Tax`-area sweep tiers for the click-only Tax 4 Continue ladder — `TAX_postreview_continue_reminder_email` (email, delay 2) + `TAX_postreview_continue_stalled` (bell, delay 4). Prior newest: `TAX_returns_received` (area Uploads, FYI bell → Tracy/Tray, added 2026-07-15). **NEWEST 2026-07-23 (migration `20260723120000`, +2 rows):** `TAX_additional_info_received` (area Uploads, FYI bell → Tracy/Tray, sort 12 — fires when a client uploads while a "Request additional information" email is outstanding, any program) + `TAX_planner_additional_info_uploaded` (area Tax Planners, FYI bell → the allocated planner, sort 26 — the SEVENTH planner bell, `default_recipients '["ALLOCATED_TAX_PLANNER"]'` placeholder resolved per-call by `notifyAllocatedPlanner`). **NEWEST 2026-07-23 evening (migration `20260723210000_impl_charged_bells`, +2 rows):** `TAX_planner_impl_charged` (area Tax Planners, FYI bell → the allocated planner, sort 27 — the EIGHTH planner bell, `["ALLOCATED_TAX_PLANNER"]` placeholder resolved per-call by `notifyAllocatedPlanner`) + `TAX_impl_charged_pf` (area Tax, FYI bell → `["ASSIGNED_PF"]`, sort 40) — both fire from `actions/tax/implementation-receipt.ts` once the implementation fee is charged and the receipt completes (`implementation_receipt_status='Sent'`); the PF bell is SKIPPED when the plan has no resolvable assigned-PF email. **NEWEST 2026-07-27 (DML only, no migration — the departed-staffer reroute, gotcha #291):** the four Tax client-decision bells `TAX_impl_client_proceed` / `TAX_impl_client_decline` / `TAX_postreview_client_proceed` / `TAX_postreview_client_refund` had their `default_recipients` retargeted from Tim Gacsy to `["ASSIGNED_PF","TAX_PLANNER"]`; `TAX_tax4_decision_needed` to those plus Tracy; `SPECIALIST_stage4_tax_risk_notes` to Tracy. No live `recipients` override named Tim. Code-side the real recipients are resolved per-call by `taxDecisionRecipients` (`utils/tax-notify.ts`) and passed as `defaultRecipients`, with `dynamic: { ASSIGNED_PF, TAX_PLANNER }` so an Editor token override resolves too. **NEWEST 2026-07-30 (migration `20260730120000_vault_request_docs.sql`, +1 row):** `VAULT_requested_doc_uploaded` (area **Uploads**, FYI bell, sort 14, label "Requested documentation uploaded (Tray)", `default_recipients '["tvaldes@elitert.com"]'` — **Tray alone, a real address rather than a role token**) — fires from `actions/vault/request-upload-notify.ts` when a client, member OR specialist uploads through a vault "Request documentation" secure link, with a per-entity deep link and the whole `notifyByRule` call wrapped in try/catch so a bell failure can never lose the file (gotcha #310). It is the FIRST rule in the new `VAULT` pipeline and is entirely separate from the `TAX_*` upload bells — a `/vault-upload` arrival raises this and nothing else. **NEWEST 2026-07-31 (migration `20260731140000_tax_retainer_paid_tray_bell.sql`, +1 row):** `TAX_retainer_paid_schedule_meeting` (area **Tax**, bell, `action_required false`, sort 15, label "Retainer paid - schedule detailed tax plan meeting", `default_recipients '["tvaldes@elitert.com"]'` — **Tray alone, a real address rather than a role token**) — fires from `actions/tax/confirmation-email.ts` at `checkout.session.completed` for BOTH card and ACH (check payments on clear, via the `checkcleared` chain), `dedupe:"unread"`, wrapped in try/catch. **It is deliberately a SEPARATE key from the existing `TAX_retainer_paid`** (the assigned-PF "Retainer paid" FYI seeded in `20260702120000`, fired from the SAME handler): one event, two audiences, two rules — reusing the key would have made this seed a silent `ON CONFLICT DO NOTHING` no-op AND coupled the two bells' `enabled`/`recipients` in the Editor (**gotcha #313**). Live-DB note: the row was first applied under the session's earlier design (recorded migration name `tax_pricing_tray_bell`, key `TAX_pricing_submitted`) and then morphed in place, so the `supabase_migrations` ledger name does not match any committed file — the file itself produces the correct end state on a rebuild. **NEWEST 2026-08-10 (migration `20260810130300_tax_roi_notification_rules.sql`, +5 rows — the tax ROI-meeting hand-off chain, gotcha #357):** **`TAX_book_roi_meeting`** (area **Tax**, bell, `action_required true`, sort 215, `default_recipients '["tvaldes@elitert.com"]'` — **Tray alone, a real address**) — **now DORMANT, see the 2026-08-11 entry below**; **`TAX_team_member_allocate_planner`** (area **Tax Planners**, bell, action-required, sort 72, `'["ALLOCATED_TAX_PLANNER"]'` placeholder) fires from `allocate-planner.ts` on **ANY** Team Member allocation, and again from `ready-for-tax3.ts` on a `confirm_date` while a Team Member still holds the plan; **`TAX_planner_assess_needed`** (area **Tax Planners**, bell, action-required, sort 74, same placeholder) fires from `allocate-planner.ts` when a **Tax Planner** is allocated with the review answered Proceed and `assess_form_submitted_at` null, and from `save-task.ts` when the review step is answered Proceed; **`TAX_assess_completed_pf`** (area **Tax**, bell, sort 216, `'["ASSIGNED_PF"]'`, **action-required since 2026-08-11**) fires from `save-assess-form.ts` on the **first** submission only (an edit stays silent, #314) and routes to the PF's **portal login** email via `taxPfLoginEmail`, not the `pf-emails.ts` correspondence map; and **`TAX_tax3_assess_reminder_email`** (area **Tax**, **kind `email`**, sort 217, `default_delay_days` **2**) gates the nightly sweep's assess reminder — **its `delay_days` means BUSINESS days BEFORE `tax3_meeting_date`, a countdown delay rather than an elapsed one** (and, since 2026-08-14, counted FORWARD — see `delay_days` below), which the Editor cannot express, so the row's own `description` carries the warning (#359). **Joined 2026-08-20 by `TAX_tax3_assess_reminder_early_email`** (area **Tax**, kind `email`, sort **214** — deliberately below 217 so the Editor lists the early tier ahead of the tier it precedes, `default_delay_days` **5**, migration `20260820141000_tax3_assess_reminder_early.sql`), the earlier tier of the same chase on its own guard column and its own `TAX_tax3_assess_reminder_early` + `|vault` templates. **Editing either delay is a live-window edit:** the early tier only fires *strictly beyond* the 2-day rule's horizon, so setting it at or below that rule's `delay_days` silently disables it — its `description` says so, since the Editor still cannot express a countdown. **The action-required rows are the ones whose TITLES are load-bearing** — `dismissible:false` means `mark_notification_read` refuses them (#179) and each is cleared by a completing handler matching **title prefix + `client_id` + pipeline + `dismissible=false`**, so rewording a title without moving its clear leaves a permanent bell (#357). One apply note for the record: the first attempt failed on `default_recipients` NOT NULL (a jsonb NULL is rejected) and rolled back cleanly before the placeholder was supplied. **NEWEST 2026-08-11 (migrations `20260811160000_tax1_reorder_planner_review_step.sql` / `20260811170000_planner_review_chain_rules.sql` / `20260811180000_tax1_request_first_and_chain_rules.sql` — +4 rows, 4 rows updated in place, 2 rows retired; the tax-track hand-off chain, gotcha #357):** **NEW —** **`TAX_planner_review_complete`** (area **Tax**, bell, action-required, sort 218, `'["tvaldes@elitert.com"]'`) is Tray's outcome bell for the "Tax planner review complete" step, with **four variants that are ALL action-required**, each retired by the handler that carries its instruction out (program-4 Proceed → Green/Red `Proceed` **and** ROI booked; program-4 Stop → the deposit refund succeeding; program-1 Proceed → ROI booked; program-1 Stop → a declined decision recorded on "ROI Meeting booked - Send Confirmation Email"); **`TAX_planner_review_needed`** (area **Tax**, bell, action-required, sort 219, `'["ALLOCATED_TAX_PLANNER"]'`) fires when a Tax Planner is allocated while the review step is unanswered; **`TAX_returns_allocate_team_member`** (area **Tax**, bell, action-required, sort 220, `'["tnmiller@elitert.com","tvaldes@elitert.com"]'` — Tracy + Tray as real addresses) fires when the client's tax returns land and nobody is allocated, and **replaces the old `TAX_returns_received` FYI**; **`TAX_planner_review_step_needed`** (area **Tax**, bell, action-required, sort 221, `'["ALLOCATED_TAX_PLANNER"]'`) fires when "Additional information required" is answered "No additional info required". **UPDATED IN PLACE —** `TAX_planner_additional_info_uploaded` and `TAX_assess_completed_pf` were flipped **`action_required true`** with new labels / descriptions / trigger notes to match call sites that now pass `dismissible:false`, and `TAX_planner_review_needed` + `TAX_team_member_allocate_planner` had their descriptions rewritten for their changed triggers. **RETIRED (rows KEPT, description prefixed `DORMANT since 2026-08-11`) —** **`TAX_book_roi_meeting`** (Tray's booking prompt now rides `TAX_planner_review_complete`), **`TAX_returns_received`**, and — added in **v725** — **`TAX_planner_returns_uploaded`**: all three have **no call site left**, which is what actually stops the bell — per gotcha #178 deleting the row would not have, and the rows are kept for rollback + Editor visibility. **`TAX_planner_returns_uploaded` is the one that reached production**: it fired on ANY sensitive-vault upload while a planner was allocated and pointed them at the Tax 2 assess step, past the Tax 1 review gate, and because its condition was "a planner is allocated" rather than "returns were requested" it also fired on additional-info responses — a planner was observed holding it and the correct review bell at once, each naming a different step. Its job is covered by `TAX_planner_review_needed` (at allocation) and the request-gated `TAX_planner_additional_info_uploaded` (when the information actually arrives). **`TAX_planner_review_step_needed` and the repurposed `TAX_planner_additional_info_uploaded` deliberately SHARE one bell title** (`Complete the tax planner review for …`): the **key** controls routing and enable-ability here in the Editor, the **title** controls clearing, so two different triggers carrying one instruction share one clear site. **NEWEST 2026-08-13** (migration `20260813203510_vault_planner_document_added_rule.sql`, **+1 row, DML only**): **`VAULT_planner_document_added`** — area **Uploads**, kind `bell`, `action_required false`, **sort 15**, label *"Planner added a document to a client vault (Tracy)"*, `default_recipients '["tnmiller@elitert.com"]'` (**Tracy as a real address, not a role token** — same pattern as `VAULT_requested_doc_uploaded`), `delay_days` / `default_delay_days` NULL, `enabled true`, seeded under `WHERE NOT EXISTS`. Fired by `utils/vault-planner-notify.ts` from BOTH signed-upload-url minters — **and, since 2026-08-27 / v796, ALSO by `notifyMemberVaultDrop` (`actions/vault/member-client-vault-shared.ts`) when a MEMBER drops a file through their client's Vault tab.** Same key, title shape, recipients and mint-time behaviour; a sibling helper rather than a branch, because `notifyPlannerVaultDrop` resolves a planner session first and silently no-ops for a member caller. **The row's own `label` / `description` / `trigger_note` still say "planner" and are OWED a one-row text update — no code change.** **It is the SECOND rule in the `VAULT` pipeline** (so `VAULT_requested_doc_uploaded` is the first *of two*, not the only one) and the first VAULT rule not tied to a `vault_upload_tokens` link — a plain admin-portal upload by a planner now raises it. **Three properties differ from every other bell here:** it fires at signed-URL **MINT** time (a failed PUT leaves one early bell), it has **NO dedupe** (every file is its own bell), and **admin uploads are excluded in code by `resolveCallerPlanner`, not by any rule setting** (#393). FYI only — no title-prefix clear site, so its title is NOT load-bearing. **NEWEST 2026-08-14** (**+2 rows, DML only** — the Tax 5 confirm-only rework): **`TAX_impl_proceed_reminder_email`** (area **Tax**, kind **`email`**, sort **131**, `default_delay_days` **2**) and **`TAX_impl_proceed_stalled`** (area **Tax**, kind **`bell`**, `action_required false`, sort **141**, `default_delay_days` **4**, to the assigned PF, title *"«Client» hasn't confirmed implementation"*). Together they are the **Proceed-branch reminder ladder that REPLACED a deleted sweep tier**: the old "Proceed pick + 24h with no client click → write `implementation_final_decision='Auto-Locked'` and fire the off-session charge" is gone, and `automation_TAX_charge_implementation` now has exactly **one runtime call site** — the client's own click. The email tier redraws the shared `TAX_implementdecision|Reminder` template with green *Proceed now* (`&decision=Confirmed`) / red *Decline implementation* (`&decision=Decline`) buttons. **Both tiers REUSE the Undecided ladder's `implementation_reminder_sent_at` / `implementation_pf_notified_at` guard columns**, which is safe because `implementation_decision` is single-valued — a row can only ever match one ladder. Like every other ladder tier their delays count **business** days (see `delay_days` below). **NEWEST 2026-08-24** (migration `20260824120000_member_revshare_hold_seeds.sql`, **+1 row**, seeded `WHERE NOT EXISTS`, already applied live): **`SPECREV_member_share_held`** — area **VFO Specialist Revenue**, kind `bell`, **`action_required true`**, sort **171**, label *"Member share held - no payout account"*, `default_recipients '["jlatham@elitert.com"]'`, `delay_days` NULL (instant), `enabled true`. Fired from `utils/specialist-revenue-payout.ts` via `notifyJakeFailure` when a Specialist Revenue payout runs for a **member** line whose member has no Stripe payout account — the line parks `awaiting_connect` and, unlike the expert path, **no account is minted and no setup email is sent**. It is the SpecRev sibling of the existing `MAP1_member_share_held` (sort 170) and `TAX_member_share_held` (sort 240); like them its **title is load-bearing** — built by `memberShareHeldTitle("Specialist Revenue", <recipient name frozen on the LINE>, <member#>)` so it reconstructs byte-identically and self-clears when the transfer lands (and on the $0-line close, that line's only chance). The same migration seeds the `MEMBERS` / `MEMBER_revshare_held` email template — see [documents.md](documents.md). **NEWEST 2026-09-08** (migration `20260908120100_ach_verify_templates_and_rules.sql`, **+2 rows**, seeded `ON CONFLICT (key) DO NOTHING`): **`MAP1_ach_bank_verification_pending`** (area **MAP 1**, kind `bell`, `action_required false`, sort **180**) and **`TAX_ach_bank_verification_pending`** (area **Tax**, kind `bell`, `action_required false`, sort **250**) — both labelled *"Client entered bank details manually (verification pending)"*, both `default_recipients '["jlatham@elitert.com","tnmiller@elitert.com"]'`, both `default_delay_days` **NULL** because they fire straight from the Stripe webhook rather than from a paced sweep, both `trigger_note` *"Stripe webhook checkout.session.completed"*. Raised by `router/webhooks.ts` when the `/pay` or `/tax-pay` ACH client typed account + routing numbers instead of signing in to their bank, leaving the PaymentIntent in `requires_action` — no money has moved and Stripe cancels the payment after about 10 days. The Tax rule serves **both** the retainer and the final-retainer fresh-link branches (the title says which). **Dismissible FYI on purpose: nothing in the portal clears them** — the client either verifies or Stripe cancels, and both outcomes arrive as their own webhook (`payment_intent.processing` / `payment_intent.canceled`), so neither title is load-bearing. The same migration also seeds the four `|ach_verify` confirmation templates (`CONTRACT_confirmationemail|ach_verify` + `TAX_confirmationemail|ach_verify`, each with its member-paying twin, recipients copied verbatim from the `|ach` rows they twin — ids 9 / 112 / 24 / 135 — and `send_mode=false`). See [../flows/stripe-webhook.md](../flows/stripe-webhook.md) and **#475**. **NEW 2026-09-17 (migration `20260917150000`, +1 row, NO DDL): `TAX_amended_invoice_not_sent`** — area *Tax*, sort **260**, `kind='bell'`, `action_required false`, `recipients` null so the code default stands, `default_recipients ["ASSIGNED_PF"]`, raised by `utils/tax-amended-invoice-chain.ts` whenever `automation_TAX_amendedinvoice` refuses, throws, or skips for any reason other than the three routine ones (*"fee not amended"*, *"revised invoice already drafted for this amendment"*, *"no email drafted"*), call-site `dedupe:"unread"` so a redelivered chain or a re-sent decision email cannot stack copies. **Dismissible FYI on purpose — nothing clears it automatically**, because re-issuing the document by hand (`scripts/tax-revised-invoice.ps1 -PlanId N`) is a judgement call, not a step a handler can complete. It exists because the refusal used to be a `console.error` and nothing else, which is how Petrus Phoa's plan 71 sat refused for eight days (**#507**). **Known nit:** the stored `description` still says the refusal happens *"when the client decision email was sent"* — stale since the trigger moved to the client's green click the same day; fix it in the Notification Editor, never by editing the applied migration file. The same migration seeds `email_templates` **273** / **274**, the no-fee implementation acknowledgement pair — see [documents.md](documents.md). **NEW 2026-09-17 (migration `20260917120000_tax_intake.sql`, +1 row, seeded `WHERE NOT EXISTS`, applied live): `FAILURE_tax_deposit_team_share`** — area **Payment Failure Alerts**, sort **52**, `kind='bell'`, **`action_required true`**, `default_recipients '["jlatham@elitert.com"]'`, `recipients` null (code default stands), `delay_days` null (instant), `enabled true`, label *"Tax deposit team share transfer failed (Jake)"*, `trigger_note` *"Green/Red Light Proceed forwards the $250 deposit team share and the Stripe transfer fails"*. Shape copied from `FAILURE_tax_planner_share`. Raised through `notifyJakeFailure` by `utils/tax-deposit-team-share.ts` when the **$250 Tax Planning Team half of a $500 tax intake deposit** cannot be forwarded to the planner's group — no group, no Connect account, transfers capability not active (probed BEFORE the transfer, #489), or Stripe refused — and the leg parks `client_tax_plans.deposit_team_share_status='Failed'` for the nightly tax revshare sweep's third pass to retry. Title `Tax deposit team share FAILED — <client> (<ref>)` (`depositTeamShareFailTitle`), reconstructable so it clears on the next success. The row exists so the alert can be re-routed or disabled from the Editor like every other bell; **it has never fired live** — the one real transfer (2026-09-18 sandbox test) succeeded first time. **NOT SEEDED, deliberately noted: `FAILURE_tax_intake_finalize`** (2026-09-18, a decision) — raised by `router/webhooks.ts` when the resumable `finalizeTaxIntake` returns `ok:false` or throws on a `paid` intake row (title `Tax intake finalize FAILED — intake <id>`, action-required, Jake by `notifyJakeFailure`'s code default), cleared by the webhook on a later success or by the 02:30 tax sweep's 4th pass when its retry succeeds; **never fired live** (the forced-failure proof was not run). **NOT SEEDED, deliberately noted: `FAILURE_tax_deposit_docs`** — the key `utils/tax-deposit-docs.ts` passes to `notifyJakeFailure` (action-required) in exactly ONE case — a freshly allocated invoice/receipt number turns out **not to be reserved** in `document_numbers` (the #282 shape); the pair is then SKIPPED so no accounting number can be duplicated, the confirmation email goes out **without attachments**, and the bell says the documents need issuing by hand. A failed PDF render or Drive upload does NOT raise it (a render failure skips the pair with a `console.error` only — numbers already stamped, no bell; an upload failure leaves the `_drive_id` NULL with the PDF still attached). **There is no `notification_rules` row for it**, so the code default (Jake) stands and it cannot be re-routed or disabled from the Editor until a row is seeded. Also never fired. Both are in the plan's known-gaps list ([plans/direct-tax-planning/README.md](../plans/direct-tax-planning/README.md)). **NEW 2026-09-21 (migration `20260921120000_tax_stop_no_deposit_template.sql`, +1 row, `on conflict (key) do nothing`): `TAX_plan_stopped_no_deposit`** — area **Tax**, sort **270**, `kind='bell'`, `action_required false`, `recipients` null so the code default stands, `default_recipients '["ASSIGNED_PF"]'`, `delay_days` null, `enabled true`, label *"Tax planning stopped (no deposit)"*. Shape copied from `TAX_deposit_refund_issued`, which is its deposit twin. Raised by `actions/tax/stop-no-deposit.ts` through `insertTaxNotifications` / `taxPfRecipients` when a **waived (no-deposit)** program-4 plan is stopped from the "Tax Plan Red Light" step after the tax planner review said Stop — the plan closes, the `TAX_stop_no_deposit` email is drafted to the client, and **no money moves** (there is no $500 to refund). Title `Tax planning stopped — <client> (<ref>)`; the message says whether the email actually drafted, because a mail failure is deliberately fail-soft and never un-stops the plan. **Dismissible FYI on purpose — nothing clears it automatically**, because the instruction it reports was already carried out by the click that raised it. The same migration seeds the `TAX_stop_no_deposit` email template — see [documents.md](documents.md). **NEW 2026-09-21 (migration `20260921140000_tax_pf_roi_decision_bells.sql`, +2 rows + 1 column, `on conflict (key) do nothing`): `TAX_roi_meeting_confirm_needed`** (area **Tax**, sort **271**) and **`TAX_client_decision_needed`** (area **Tax**, sort **272**) — both `kind='bell'`, **`action_required true`**, `recipients` null so the code default stands, `default_recipients '["ASSIGNED_PF"]'`, `delay_days` null, `enabled true`. Shape copied from `TAX_plan_stopped_no_deposit` but action-required, because each carries one instruction and one handler clears it. They are the two PF asks that bracket the ROI meeting. **Their Direct-plan routing DIFFERS, deliberately (Jake, 2026-09-21):** `TAX_roi_meeting_confirm_needed` follows the member like every other `ASSIGNED_PF` bell (`taxPfRecipientsFor`, see [../flows/notifications.md](../flows/notifications.md)), but **`TAX_client_decision_needed` goes to the ALLOCATED TAX PLANNER and TEAM MEMBER on a Direct plan** — through `notifyAllocatedPlanner`, with the planner-portal link, exactly how `TAX_planner_assess_needed` is addressed — because on Direct the planner records the client's decision, not the member. It is one of **four** PF bells that do not follow the member on Direct — `TAX_roi_skipped_decision_needed` (both fire sites) and, since v873 (#521), `TAX_client_decided_yes` + `TAX_client_extra_meeting_requested` in `final-decision.ts` are the others, each asking for a step that is the planner's on Direct (the decision, the pricing form, the extra-meeting outcome); a bell is addressed to the OWNER of the step it asks for. Its `default_recipients` is `'["ASSIGNED_PF","ALLOCATED_TAX_PLANNER"]'` to show both audiences, and the **title is identical on both routes** so the single clear site retires either vintage (#411). A Direct plan with no allocated planner raises nothing. `TAX_roi_meeting_confirm_needed` — title `Confirm the ROI meeting happened for <client>` — is raised by the **5-minute reminder sweep** (`actions/reminders/sweep.ts`, cron `reminder-sweep-5min`, jobid 14) once the booked meeting instant has passed and the "ROI Presentation" step is still unanswered; cleared when that step is answered with any value, or when the plan closes (refund / no-deposit stop / ROI meeting skipped). `TAX_client_decision_needed` — title `Record the client's tax planning decision for <client>` — is raised by `actions/tax/save-task.ts` the moment "ROI Presentation" is answered with no decision recorded; cleared by any decision in `actions/tax/decision.ts`, beside the existing `TAX_roi_skipped_decision_needed` clear it shares that site with. **The same migration adds the one column the first rule needs:** `client_tax_plans.roi_confirm_bell_sent_at timestamptz` — the sweep's once-per-plan latch. The sweep selects `roi_confirm_bell_sent_at is null` only and stamps AFTER the insert, so a thrown insert retries on the next tick rather than latching away silently; **re-booking the ROI meeting does NOT reset it**, because the bell asks whether the meeting happened and a rescheduled plan is answered on the step, not by a second bell. **NEW 2026-09-23 (migration `20260923180100_tax_diagnostic_templates_rule.sql`, +1 row, `on conflict (key) do nothing`): `TAX_diagnostic_submitted`** — area **Tax**, sort **273**, `kind='bell'`, **`action_required true`**, `recipients` null, `default_recipients '["tnmiller@elitert.com","eanderson@elitert.com","platham@elitert.com","jlatham@elitert.com"]'` (Tracy, Evan, Paul, Jake — LOGIN addresses), label *"New VFO Tax Diagnostic waiting in the queue"*. Raised by `tax_diagnostic_submit` for every public VFO Tax Diagnostic, title `New Tax Diagnostic — <client>`, link `/admin?tab=tax_diagnostics&diag=<id>`; **cleared by its LINK** (not a title prefix) by `tax_diagnostic_confirm` / `tax_diagnostic_dismiss`, so two diagnostics for one client name clear independently. See [../flows/tax-diagnostic.md](../flows/tax-diagnostic.md). **NEW 2026-09-23 (migration `20260923220000_tax_intake_deposit_ach.sql`, +2 rows, `on conflict (key) do nothing`) — the $500 tax intake deposit went card-or-ACH:** **`FAILURE_tax_intake_deposit_ach`** — area **Payment Failure Alerts**, sort **54**, `kind='bell'`, **`action_required true`**, `default_recipients '["jlatham@elitert.com"]'`, label *"Tax intake deposit ACH payment failed (Jake)"*, `trigger_note` *"Stripe webhook payment_intent.payment_failed / payment_intent.canceled / checkout.session.async_payment_failed (payment_kind tax_intake_deposit)"*. Raised through `notifyJakeFailure` by `utils/tax-intake-deposit-webhook.ts` when a BOOKED ACH deposit bounces, fails or its micro-deposit verification expires (title `Tax intake deposit ACH FAILED — intake <id>`: no case was created, the intake row is released so the payer's original link works again) — **never for an abandoned, unbooked Checkout (#482)** — and, under a different title, when a Checkout completes on a session the row has already moved past (*paid on a superseded Checkout*). Cleared (`clearJakeFailure`) when that intake is later paid by card or settles. **`TAX_intake_deposit_verification_pending`** — area **Tax**, sort **274**, `kind='bell'`, `action_required false`, `default_recipients '["jlatham@elitert.com","tnmiller@elitert.com"]'` (the audience of `TAX_ach_bank_verification_pending`), label *"Tax intake deposit: bank details entered manually (verification pending)"*. Raised at `checkout.session.completed` when the deposit's ACH PaymentIntent is `requires_action` (manual account entry, micro-deposits pending, nothing debited), `dismissible:true`, `dedupe:"unread"`; **dismissible FYI — nothing clears it**, the payer verifies (the deposit then settles and the case is created) or Stripe cancels (which raises the failure bell). Both have **never fired on a real (non-sandbox) payment**; the manual-entry path was exercised in the 2026-09-23 sandbox test (code `SM11AA`). The same migration seeds the ACH confirmation templates 289 / 290 — see [documents.md](documents.md).

| Column | Type | Notes |
|---|---|---|
| `key` | text | pk — the ruleKey passed to `notifyByRule` / `getRuleConfig` (e.g. `MAP1_pcadmin_decision_yes`, `TAX_tax3_payment_stalled`) |
| `area` | text | Editor grouping ("MAP 1", "Tax", …, "Uploads", "Payment Failure Alerts") |
| `label` / `description` / `trigger_note` | text | Human-readable what/when |
| `kind` | text | `'bell'` or `'email'` — email rows gate a sweep reminder-email's timing/on-off only; their recipient overrides are ignored (gotcha #177; wording lives in Email Templates) |
| `action_required` | boolean | **DISPLAY / DOCUMENTATION ONLY.** `utils/notify.ts` never reads it and never lets a rule override the call site's `dismissible` — flipping an action-required bell to FYI (or back) from the Editor would break the contract that a completing handler clears it. The CODE's `dismissible` flag is authoritative; this column exists so the Editor can label the row honestly, and the two are kept in step **by hand** (this session's DML updated four rows to match code that had already changed). |
| `recipients` | jsonb | Override list — emails, `'admin'`, `'all'`, or dynamic tokens `ASSIGNED_PF` / `TEAM_MEMBER` / `ASSIGNED_ADMIN` / `TAX_PLANNER` / `ALLOCATED_TAX_PLANNER` (resolved per event — **note the last two name the same concept but are NOT interchangeable; pass whichever token the rule itself carries**, gotcha #291). **null = use the code default.** **Ignored on a DIRECT tax plan fire (2026-09-21):** a call site passing `forceRecipients` routes to its code default verbatim, because this list can only ever name VFO logins and a Direct plan's PF is a member; `enabled` and `delay_days` still apply |
| `default_recipients` | jsonb | The code default, for display + reset |
| `delay_days` / `default_delay_days` | numeric | Sweep tiers only (defaults 2 email / 4 bell / 14 check-uncleared / 7 check-lookahead); null default = instant, not delay-editable. **The unit is BUSINESS days as of 2026-08-14 — Mon–Fri UTC, no holiday calendar.** The stored numbers did not change; only the counting did. `utils/notify.ts` **`businessDelayCutoffIso(days)`** walks weekdays **backward** from `now()` and drives every reminder ladder; **`businessDayHorizonDateOnly(fromDateOnly, days)`** walks them **forward** and drives the countdown/lookahead horizons — the `TAX_tax3_assess_reminder_email` days-*before*-the-booked-meeting window (#359), its 2026-08-20 early sibling `TAX_tax3_assess_reminder_early_email` (default 5, whose window is the range *past* the 2-day horizon), and the MAP 1 check-payment lookahead. Bell messages now read literally *"N business day(s) have passed"*. The old calendar helper **`delayCutoffIso` still exists with ZERO callers** — never wire a new tier to it. **Sweeps converted:** tax `revshare-sweep` (Tax 3, Tax 4, Tax 5), `contract-revshare-sweep` (MAP 1), `advisor/sweep`, `accountant/sweep`, `onboarding/sweep`, `pft/sweep`, `specialist-revenue/payout-sweep`, `contract-check-reminder-sweep`, `regular/map4-followup-sweep`; `growth/overdue-sweep` carries no delay offset at all and instead **early-returns on Saturday and Sunday**. **DELIBERATE CALENDAR SURVIVORS — the unit is NOT global, and these were left alone on purpose:** the advisor + accountant **14-day auto-decline**, the Tax 4 "meeting has passed" nudge, the membership **30-day renewal notice**, the `chargescheduled` sweep (charges land on real due dates, weekends included), the notifications purge, personal reminders, and every token / session expiry window. |
| `enabled` | boolean | `false` suppresses the notification / skips the sweep tier (BEFORE its idempotency stamp). Deleting a row does NOT disable — gotcha #178 |
| `sort` | integer | Order within the area |

**Frontend:** [NotificationBell.jsx](src/components/NotificationBell.jsx) polls every 30s + listens for `vfo:notifications-changed`. Compact rows: action-required sorted first with orange left bar + ACTION pill, wrapping title, **the full `message` rendered inline below it on wrapping lines** (2026-08-11 — it used to be a hover tooltip; reminder rows keep the violet bar + REMINDER pill), then pipeline chip + date meta; header shows "N need action"; badge turns orange when anything needs action. **2026-07-22 (gotcha #267):** the row-click handler now marks-read + removes the row locally BEFORE navigating (two bell instances share a click path — portal header + `ClientDetail` header — so navigating first let the destination bell's mount-load resurrect the just-clicked row); dropdown widened 380→480px with wrapping titles; "View all" is hidden for `tax_planner` sessions (it routes to `/admin`).
